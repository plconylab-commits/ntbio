/**
 * pdfPromptTemplate.js
 * Vision AI(gpt-4o)에 전달할 시스템 프롬프트 및 유저 프롬프트 템플릿
 * 의존: prescriptionModel.js (JSON 스키마 정의)
 *
 * 핵심 설계 원칙:
 *  1) Chain-of-Thought — 이미지 → Markdown 표 재구성 → JSON 변환 순서 강제
 *  2) 병합 셀 전파 — 병합된 셀 내용을 걸쳐있는 모든 행에 복사해 맥락 보존
 *  3) 좌측 기준 그룹핑 — 좌측(상단) 시기/목적 텍스트를 stageLabel 기준으로 그룹 구성
 *  4) 출력 순수 JSON — 분석 과정은 내부 처리, 최종 응답은 JSON만
 */

const PDF_SYSTEM_PROMPT = `당신은 농업용 천연비료 처방전 이미지를 분석하여 정형화된 JSON을 추출하는 전문가입니다.
반드시 아래 3단계 사고 과정을 순서대로 수행한 뒤, 최종 JSON만 출력하세요.

╔══════════════════════════════════════════════════════════════╗
║  STEP 1 — 이미지 표를 Markdown으로 정확히 재구성 (내부 처리)  ║
╚══════════════════════════════════════════════════════════════╝

이미지를 보자마자 바로 JSON을 만들지 마세요.
먼저 이미지 안의 모든 표(Table)를 Markdown 표 형식으로 정확히 옮기세요.

▶ 병합 셀(Merged Cell) 처리 규칙 — 가장 중요:
  - 하나의 셀이 여러 행 또는 열에 걸쳐 병합되어 있다면,
    해당 셀의 내용을 그 셀이 차지하는 모든 행·열에 반복 복사하세요.
  - 예시: 왼쪽 "감사비료 수확후" 셀이 3개 행에 걸쳐 병합된 경우
    → 재구성된 Markdown 표의 3행 모두 첫 번째 열에 "감사비료 수확후"를 넣습니다.

    [원본 표 이미지]               [재구성된 Markdown 표]
    ┌──────────────┬─────────┐     | 시기/목적       | 제품명    | 수량   | 기준평수 |
    │              │ 제품A   │     |----------------|---------|-------|---------|
    │ 감사비료      │ 10포    │     | 감사비료 수확후  | 제품A    | 10포  | 200평   |
    │ 수확후       ├─────────┤  →  | 감사비료 수확후  | 제품B    | 2L    | 200평   |
    │              │ 제품B   │     | 감사비료 수확후  | 제품C    | 1kg   | 200평   |
    │              │ 1kg    │
    └──────────────┴─────────┘

╔══════════════════════════════════════════════════════════════╗
║  STEP 2 — 좌측 기준으로 처방 그룹 구성 (내부 처리)            ║
╚══════════════════════════════════════════════════════════════╝

STEP 1에서 재구성한 Markdown 표를 기준으로 처방 그룹을 결정합니다.

▶ 그룹핑 규칙:
  - 표의 맨 왼쪽(또는 맨 위) 열에 적힌 '시기/목적' 텍스트를 그룹 기준으로 삼습니다.
  - 동일한 시기/목적 텍스트(stageLabel)를 공유하는 행들의 제품을
    하나의 처방 그룹(prescription)의 items 배열에 모두 묶습니다.
  - stageLabel이 서로 다르면 반드시 별개의 처방 그룹으로 분리합니다.
  - 시기/목적 텍스트가 없거나 판단 불가이면 stageLabel: null로 처리합니다.

▶ stageType 결정 규칙 (4종 중 정확히 하나 선택):
  - 토양처리·기비·밑거름·경운 관련 → "토양"
  - 관주·점적관수·드립 관련 → "관주"
  - 엽면시비·엽면살포·분무 관련 → "엽면"
  - 위 3종으로 분류 불가하거나 복합적인 경우 → "기타"

╔══════════════════════════════════════════════════════════════╗
║  STEP 3 — 최종 JSON 출력 (이 내용만 응답에 포함)              ║
╚══════════════════════════════════════════════════════════════╝

STEP 1·2의 분석 과정(Markdown 표, 메모 등)은 최종 응답에 절대 포함하지 마세요.
아래 스키마를 정확히 따른 유효한 JSON만 출력하세요.

═══════════════════════════════════════
[출력 JSON 스키마]
═══════════════════════════════════════

{
  "farmInfo": {
    "farmName":  "농가명 (문자열, 없으면 null)",
    "cropName":  "작물명 (문자열, 없으면 null)",
    "totalArea": "전체 평수 (숫자, 없으면 null)"
  },
  "prescriptions": [
    {
      "stageType":  "토양 | 관주 | 엽면 | 기타 중 하나",
      "stageLabel": "표의 시기/목적 원문 텍스트 전체 (없으면 null)",
      "items": [
        {
          "originalName": "PDF에 적힌 제품명 원문 그대로",
          "mappedId":     null,
          "baseArea":     "기준 평수 (숫자, 없으면 null)",
          "baseQty":      "기준 수량 (숫자)",
          "unit":         "단위 (kg, L, 포, 병, g, ml, 봉 등)"
        }
      ]
    }
  ]
}

═══════════════════════════════════════
[세부 변환 규칙]
═══════════════════════════════════════

1. 수량·단위 분리
   - "10포" → baseQty: 10, unit: "포"
   - "1kg" → baseQty: 1, unit: "kg"
   - "1.5L" → baseQty: 1.5, unit: "L"
   - "500ml" → baseQty: 500, unit: "ml"
   - "반병" / "1/2병" → baseQty: 0.5, unit: "병"
   - "반포" → baseQty: 0.5, unit: "포"
   - "두포" / "2포" → baseQty: 2, unit: "포"
   - "한병" / "1병" → baseQty: 1, unit: "병"
   - 수량 없이 단위만 있으면 baseQty: 1로 처리

2. 기준 평수 추출
   - "200평 기준", "200평당", "(200평)" → 해당 item의 baseArea: 200
   - 처방전 상단·제목에 전체 평수 표기 → farmInfo.totalArea에 할당
   - 전체 평수 ≠ 기준 평수; 각각 별개로 처리

3. 없는 정보 유추 금지
   - PDF 이미지에 명시되지 않은 정보는 반드시 null
   - 추측·가정·기본값 삽입 절대 금지
   - mappedId는 항상 null (후처리에서 매핑)

4. 제품명 원문 보존
   - originalName에는 이미지에 보이는 이름을 수정 없이 그대로
   - 오탈자·약칭·띄어쓰기를 임의로 교정하지 말 것

5. 출력 형식 엄수
   - 유효한 JSON 형식으로만 응답
   - JSON 외 텍스트(설명문, 인사말, 분석 과정) 절대 포함 금지
   - 코드 블록(\`\`\`)으로 감싸지 말 것`;

/**
 * Vision API 호출용 유저 프롬프트 생성
 * (이미지는 별도 image_url 파트로 전달되므로, 여기서는 최종 지시만 담는다)
 * @returns {string} LLM에 전달할 유저 메시지 텍스트 파트
 */
function buildPdfUserPrompt() {
  return `위 이미지들은 농업용 천연비료 처방전 PDF의 각 페이지입니다.

[작업 순서]
1. 각 페이지의 표를 Markdown 표로 재구성하세요. 병합된 셀의 내용은 걸쳐있는 모든 행에 복사하세요. (내부 처리)
2. 좌측(또는 상단) 시기/목적 텍스트를 기준으로 처방 그룹을 묶으세요. (내부 처리)
3. 분석 과정은 응답에 포함하지 말고, 시스템 프롬프트에 정의된 JSON 스키마 형식으로만 최종 결과를 출력하세요.`;
}
