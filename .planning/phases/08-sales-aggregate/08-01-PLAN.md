---
phase: 08-sales-aggregate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - salesHistoryUI.js
  - index.html
autonomous: true
requirements:
  - SALE-04
  - SALE-05

must_haves:
  truths:
    - "매출 집계 패널에 '이번 분기'와 '지난 분기' 버튼이 표시된다"
    - "분기 버튼 클릭 시 해당 기간의 거래건수·총매출·입금액·미수금이 표시된다"
    - "1월에 '지난 분기' 클릭 시 전년도 4분기(10~12월) 범위로 계산된다"
    - "고객별 집계 패널이 열리면 전체 고객의 거래금액·미수금이 목록으로 표시된다"
    - "고객별 집계 목록은 미수금 내림차순으로 정렬된다"
    - "삭제됨 상태 거래는 집계에서 제외된다"
  artifacts:
    - path: "salesHistoryUI.js"
      provides: "_thisQuarterRange(), _lastQuarterRange(), _buildCustomerAggregates(), openCustomerSummaryPanel()"
      contains: "thisQuarterRange"
    - path: "salesHistoryUI.js"
      provides: "SalesHistoryUI.openCustomerSummaryPanel 공개 API"
      contains: "openCustomerSummaryPanel"
    - path: "index.html"
      provides: "고객별 집계 진입점 버튼"
      contains: "openCustomerSummaryPanel"
  key_links:
    - from: "분기 버튼 클릭 핸들러"
      to: "_thisQuarterRange() / _lastQuarterRange()"
      via: "_activePeriod === 'thisQ' / 'lastQ' 분기"
      pattern: "thisQ.*lastQ"
    - from: "openCustomerSummaryPanel()"
      to: "_buildCustomerAggregates()"
      via: "getInvoiceHistory() IndexedDB 캐시"
      pattern: "_buildCustomerAggregates"
---

<objective>
매출 집계 패널에 분기 단위 조회 버튼을 추가하고(SALE-04), 고객별 총 거래금액·미수금을 한눈에 볼 수 있는 집계 뷰를 추가한다(SALE-05).

Purpose: v1.0에서 확인된 매출 집계 기능 공백 두 가지를 채운다. 사용자가 분기 단위로 매출을 조회하고, 미수금이 많은 고객을 한눈에 파악할 수 있어야 한다.
Output: salesHistoryUI.js에 분기 범위 함수 2개 + 고객별 집계 함수 1개 + 오버레이 UI, index.html에 진입점 버튼 1개
</objective>

<execution_context>
@/Users/glen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sales-aggregate/08-RESEARCH.md

<interfaces>
<!-- salesHistoryUI.js 현재 구조 (357 lines, IIFE 패턴) -->

내부 유틸 함수 (재사용 가능):
- _dateStr(d)           → 'YYYY-MM-DD' 문자열
- _thisMonthRange()     → { from: 'YYYY-MM-DD', to: 'YYYY-MM-DD' }
- _lastMonthRange()     → { from: 'YYYY-MM-DD', to: 'YYYY-MM-DD' }
- _calcPeriodSales(dateFrom, dateTo) → { count, total, paid, unpaid, invoices }
- _fmt(n)              → comma(n) 래퍼, 원화 포맷
- _createSalesOverlay() → 오버레이 DOM 생성 및 내부 변수 초기화
- openSalesPanel()      → 오버레이 열기 (이번 달 기본값)
- _closeSalesPanel()    → 오버레이 닫기

현재 기간 버튼 핸들러 (salesHistoryUI.js line 199–206):
```javascript
if (_activePeriod === 'custom') {
  _dateRangeEl.classList.add('on');
} else {
  _dateRangeEl.classList.remove('on');
  var range = _activePeriod === 'this' ? _thisMonthRange() : _lastMonthRange();
  _renderSalesStats(range.from, range.to);
}
```
→ 분기 추가 시 else 분기 내 range 계산 로직을 if-else 체인으로 교체해야 함

현재 공개 API (window.SalesHistoryUI):
```javascript
window.SalesHistoryUI = {
  openSalesPanel:         openSalesPanel,
  updateCustomerSummary:  _updateCustomerSummary
};
```

index.html 액션 버튼 영역 (line ~699):
```html
<div class="act-row">
  <button ... onclick="SalesHistoryUI.openSalesPanel()">매출 집계</button>
  <button ... onclick="checkUnpaidOnCustomer()">미수금 확인</button>
</div>
```

전역 함수 (index.html에서 노출):
- getInvoiceHistory()   → IndexedDB 캐시 배열 (_invoiceCache)
- customerKey(name, phone) → '이름|전화번호' 정규화 키
- comma(n)             → 원화 콤마 포맷
- openAllUnpaid()      → 전체 미수금 목록 오버레이 열기
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SALE-04 — 분기 범위 함수 + 버튼 + 핸들러 수정</name>
  <files>salesHistoryUI.js</files>
  <read_first>
    - salesHistoryUI.js: 전체 파일 (357 lines) — _lastMonthRange 함수 위치(line 85~90), _createSalesOverlay 내 버튼 HTML(line 143~145), 기간 버튼 클릭 핸들러(line 191~207)
  </read_first>
  <action>
salesHistoryUI.js에 세 가지 변경을 적용한다 (바닐라 JS, IIFE 내부, var 선언):

**변경 1: 분기 범위 함수 2개 추가**

`_lastMonthRange()` 함수(line 85) 바로 아래에 추가:

```javascript
function _thisQuarterRange() {
  var now = new Date();
  var q = Math.floor(now.getMonth() / 3);
  var from = new Date(now.getFullYear(), q * 3, 1);
  var to   = new Date(now.getFullYear(), q * 3 + 3, 0);
  return { from: _dateStr(from), to: _dateStr(to) };
}

function _lastQuarterRange() {
  var now = new Date();
  var q = Math.floor(now.getMonth() / 3);
  var lastQ = q - 1;
  var year = now.getFullYear();
  if (lastQ < 0) { lastQ = 3; year -= 1; }
  var from = new Date(year, lastQ * 3, 1);
  var to   = new Date(year, lastQ * 3 + 3, 0);
  return { from: _dateStr(from), to: _dateStr(to) };
}
```

**변경 2: 기간 버튼 HTML에 분기 버튼 2개 추가**

`_createSalesOverlay()` 내 `.sales-period-btns` div 안에서 `'직접 입력'` 버튼 줄 바로 다음에 추가:

```javascript
'    <button class="sales-period-btn" data-period="thisQ">이번 분기</button>',
'    <button class="sales-period-btn" data-period="lastQ">지난 분기</button>',
```

**변경 3: 기간 버튼 클릭 핸들러 수정**

`_createSalesOverlay()` 내 기간 버튼 클릭 핸들러의 else 분기(현재 `var range = _activePeriod === 'this' ? _thisMonthRange() : _lastMonthRange();` 한 줄)를 다음으로 교체:

```javascript
var range;
if (_activePeriod === 'this')       range = _thisMonthRange();
else if (_activePeriod === 'last')  range = _lastMonthRange();
else if (_activePeriod === 'thisQ') range = _thisQuarterRange();
else if (_activePeriod === 'lastQ') range = _lastQuarterRange();
if (range) _renderSalesStats(range.from, range.to);
```

주의사항:
- 기존 `_renderSalesStats(range.from, range.to);` 호출 줄을 그대로 유지하지 말고 위 if(range) 패턴으로 완전 교체
- IIFE 내 `'use strict'` 환경이므로 var 선언 사용
  </action>
  <verify>
    <automated>grep -n "thisQuarterRange\|lastQuarterRange\|thisQ\|lastQ" /Users/glen/천연비료처방전/천연비료처방전/.claude/worktrees/hopeful-kepler/salesHistoryUI.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "function _thisQuarterRange" salesHistoryUI.js → 1줄 출력 (함수 정의 존재)
    - grep "function _lastQuarterRange" salesHistoryUI.js → 1줄 출력 (함수 정의 존재)
    - grep 'data-period="thisQ"' salesHistoryUI.js → 1줄 출력 (버튼 HTML 존재)
    - grep 'data-period="lastQ"' salesHistoryUI.js → 1줄 출력 (버튼 HTML 존재)
    - grep "_activePeriod === 'thisQ'" salesHistoryUI.js → 1줄 출력 (핸들러 분기 존재)
    - grep "_activePeriod === 'lastQ'" salesHistoryUI.js → 1줄 출력 (핸들러 분기 존재)
  </acceptance_criteria>
  <done>분기 버튼 2개가 매출 집계 패널에 렌더되고, 클릭 시 해당 분기 범위로 _renderSalesStats가 호출된다. 1월 기준 _lastQuarterRange()는 전년도 10월 1일 ~ 12월 31일을 반환한다.</done>
</task>

<task type="auto">
  <name>Task 2: SALE-05 — 고객별 집계 함수 + 오버레이 UI + 진입점 버튼</name>
  <files>salesHistoryUI.js, index.html</files>
  <read_first>
    - salesHistoryUI.js: line 1~68 (CSS 주입 함수 _injectSalesCSS) — CSS 추가 위치 확인
    - salesHistoryUI.js: line 350~357 (window.SalesHistoryUI 공개 API) — openCustomerSummaryPanel 추가 위치
    - index.html: "매출 집계" 버튼이 있는 act-row 영역 — "고객별 집계" 버튼 추가 위치
  </read_first>
  <action>
**salesHistoryUI.js 변경 (4곳):**

**변경 A: CSS 추가** — `_injectSalesCSS()` 내 `style.textContent = [...]` 배열 마지막 항목(`.cust-sales-badge.has-unpaid` 줄) 뒤에 다음 CSS 항목들을 추가:

```javascript
'.cust-summary-overlay {',
'  position:fixed; inset:0; background:rgba(0,0,0,.45); z-index:9001;',
'  display:none; align-items:center; justify-content:center;',
'}',
'.cust-summary-overlay.on { display:flex; }',
'.cust-summary-panel {',
'  background:#fff; max-width:560px; width:92%; max-height:82vh;',
'  overflow-y:auto; border-radius:14px; padding:24px; position:relative;',
'  box-shadow:0 8px 32px rgba(0,0,0,.2);',
'}',
'.cust-summary-panel h3 { font-size:18px; font-weight:700; margin-bottom:16px; }',
'.cust-summary-table { width:100%; border-collapse:collapse; font-size:13px; }',
'.cust-summary-table th {',
'  text-align:left; padding:6px 8px; border-bottom:2px solid var(--border);',
'  font-weight:700; color:var(--muted); font-size:12px;',
'}',
'.cust-summary-table td { padding:7px 8px; border-bottom:1px solid #f0f0f0; }',
'.cust-summary-table tr:last-child td { border-bottom:none; }',
'.cust-summary-unpaid { color:#C62828; font-weight:700; }',
'.cust-summary-empty { text-align:center; color:var(--muted); padding:32px 0; }',
```

**변경 B: `_buildCustomerAggregates()` 함수 추가** — `_closeSalesPanel()` 함수(line 259) 아래에 추가:

```javascript
function _buildCustomerAggregates() {
  var history = (typeof getInvoiceHistory === 'function') ? getInvoiceHistory() : [];
  var fmtKey = (typeof customerKey === 'function')
    ? customerKey
    : function(n, p) { return (n.trim() + '|' + p.replace(/\D/g, '')).toLowerCase(); };
  var map = {};
  history.forEach(function(h) {
    if (h.status === '삭제됨') return;
    var key = fmtKey(h.customer.name, h.customer.phone);
    if (!map[key]) {
      map[key] = {
        name: h.customer.name,
        phone: h.customer.phone,
        region: h.customer.region || '',
        count: 0, total: 0, paid: 0
      };
    }
    var entry = map[key];
    entry.count++;
    entry.total += (h.totals && h.totals.grandTotal) || 0;
    var paidAmt = h.payments
      ? h.payments.filter(function(p) { return !p.cancelled; })
                  .reduce(function(s, p) { return s + p.amount; }, 0)
      : (h.paidAmount || 0);
    entry.paid += paidAmt;
  });
  return Object.keys(map).map(function(k) {
    var e = map[k];
    e.unpaid = e.total - e.paid;
    return e;
  }).sort(function(a, b) { return b.unpaid - a.unpaid; });
}
```

**변경 C: `openCustomerSummaryPanel()` 함수 추가** — `_buildCustomerAggregates()` 아래에 추가:

```javascript
var _custSummaryOverlay = null;

function openCustomerSummaryPanel() {
  if (!_custSummaryOverlay) {
    _custSummaryOverlay = document.createElement('div');
    _custSummaryOverlay.id = 'custSummaryOverlay';
    _custSummaryOverlay.className = 'cust-summary-overlay';
    _custSummaryOverlay.innerHTML = [
      '<div class="cust-summary-panel">',
      '  <button class="sales-close-btn" id="custSummaryCloseBtn">✕</button>',
      '  <h3>고객별 집계</h3>',
      '  <div id="custSummaryBody"></div>',
      '</div>'
    ].join('\n');
    document.body.appendChild(_custSummaryOverlay);
    _custSummaryOverlay.addEventListener('click', function(e) {
      if (e.target === _custSummaryOverlay) _custSummaryOverlay.classList.remove('on');
    });
    document.getElementById('custSummaryCloseBtn').addEventListener('click', function() {
      _custSummaryOverlay.classList.remove('on');
    });
  }

  var rows = _buildCustomerAggregates();
  var body = document.getElementById('custSummaryBody');

  if (!rows.length) {
    body.innerHTML = '<div class="cust-summary-empty">거래 이력이 없습니다.</div>';
  } else {
    var html = [
      '<table class="cust-summary-table">',
      '<thead><tr>',
      '<th>고객명</th><th>지역</th><th>거래</th><th>총 매출</th><th>미수금</th>',
      '</tr></thead><tbody>'
    ];
    rows.forEach(function(r) {
      var unpaidClass = r.unpaid > 0 ? ' class="cust-summary-unpaid"' : '';
      html.push(
        '<tr>',
        '<td>' + r.name + '</td>',
        '<td>' + (r.region || '-') + '</td>',
        '<td>' + r.count + '건</td>',
        '<td>' + _fmt(r.total) + '원</td>',
        '<td' + unpaidClass + '>' + _fmt(r.unpaid) + '원</td>',
        '</tr>'
      );
    });
    html.push('</tbody></table>');
    body.innerHTML = html.join('');
  }

  _custSummaryOverlay.classList.add('on');
}
```

**변경 D: window.SalesHistoryUI 공개 API 확장** — 기존 `window.SalesHistoryUI = {...}` 객체에 `openCustomerSummaryPanel` 추가:

```javascript
window.SalesHistoryUI = {
  openSalesPanel:             openSalesPanel,
  updateCustomerSummary:      _updateCustomerSummary,
  openCustomerSummaryPanel:   openCustomerSummaryPanel
};
```

**index.html 변경:**

"매출 집계" 버튼이 있는 `act-row` div 안에서 `SalesHistoryUI.openSalesPanel()` 버튼 바로 다음에 "고객별 집계" 버튼을 추가한다. 기존 버튼과 동일한 스타일 클래스를 사용:

```html
<button class="act-btn" onclick="SalesHistoryUI.openCustomerSummaryPanel()">고객별 집계</button>
```

실제 추가 위치는 index.html에서 `openSalesPanel` 검색으로 확인 후 해당 버튼 다음 줄에 삽입. 버튼 클래스는 주변 버튼과 동일하게 맞출 것.
  </action>
  <verify>
    <automated>grep -n "openCustomerSummaryPanel\|_buildCustomerAggregates\|cust-summary-overlay\|cust-summary-table" /Users/glen/천연비료처방전/천연비료처방전/.claude/worktrees/hopeful-kepler/salesHistoryUI.js && grep -n "openCustomerSummaryPanel" /Users/glen/천연비료처방전/천연비료처방전/.claude/worktrees/hopeful-kepler/index.html</automated>
  </verify>
  <acceptance_criteria>
    - grep "function _buildCustomerAggregates" salesHistoryUI.js → 1줄 출력
    - grep "function openCustomerSummaryPanel" salesHistoryUI.js → 1줄 출력
    - grep "openCustomerSummaryPanel" salesHistoryUI.js → 최소 2줄 출력 (함수 정의 + 공개 API)
    - grep "cust-summary-overlay" salesHistoryUI.js → 최소 2줄 출력 (CSS + 함수 내 사용)
    - grep "cust-summary-table" salesHistoryUI.js → 최소 2줄 출력 (CSS + HTML 생성)
    - grep "h.payments" salesHistoryUI.js → 최소 1줄 출력 (_buildCustomerAggregates 내 payments 배열 기반 계산)
    - grep "openCustomerSummaryPanel" index.html → 1줄 출력 (진입점 버튼 존재)
    - grep "openCustomerSummaryPanel.*SalesHistoryUI" salesHistoryUI.js → 1줄 출력 (공개 API에 추가됨)
  </acceptance_criteria>
  <done>
    - 고객별 집계 버튼이 index.html 액션 버튼 영역에 표시된다
    - 버튼 클릭 시 오버레이가 열리며 고객명·지역·거래건수·총매출·미수금 컬럼 테이블이 표시된다
    - 미수금 금액은 빨간색으로 강조된다
    - 목록은 미수금 내림차순 정렬된다
    - 삭제됨 거래는 집계에서 제외된다
    - payments 배열이 있는 레코드는 cancelled 제외 후 계산한다
  </done>
</task>

</tasks>

<verification>
브라우저에서 index.html을 열고:
1. "매출 집계" 버튼 클릭 → 패널에 "이번 분기", "지난 분기" 버튼 2개 추가 확인
2. "이번 분기" 클릭 → 이번 분기(예: 1분기 = 1~3월) 거래 집계 표시 확인
3. "지난 분기" 클릭 → 이전 분기 집계 표시 확인 (1월이면 전년도 4분기 10~12월)
4. "고객별 집계" 버튼 클릭 → 오버레이 열리며 고객 목록 테이블 표시 확인
5. 미수금이 있는 고객은 미수금 컬럼이 빨간색으로 표시 확인
6. 목록이 미수금 내림차순으로 정렬 확인
</verification>

<success_criteria>
- salesHistoryUI.js에 _thisQuarterRange, _lastQuarterRange 함수 존재
- 매출 집계 패널에 분기 버튼 2개 표시 (이번 분기, 지난 분기)
- 분기 버튼 클릭 시 해당 기간 매출 집계 표시
- 고객별 집계 버튼이 index.html에 존재하고 openCustomerSummaryPanel 호출
- 고객별 집계 오버레이에 전체 고객 목록이 미수금 내림차순으로 표시
- payments.cancelled 제외 정확 계산 적용
</success_criteria>

<output>
After completion, create `.planning/phases/08-sales-aggregate/08-01-SUMMARY.md`
</output>
