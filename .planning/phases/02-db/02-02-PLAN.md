---
phase: 02-db
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - customerUI.js
  - index.html
autonomous: false
requirements:
  - CUST-02
  - CUST-03
  - CUST-05
  - DISC-01
  - DISC-02

must_haves:
  truths:
    - "#cName 입력 시 1자 이상이면 자동완성 드롭다운이 표시된다"
    - "드롭다운에서 고객 선택 시 #cName, #cPhone, #cAddr, #cArea, #cCrop, #gDisc가 채워진다"
    - "고객 선택 시 저장된 할인율이 #gDisc에 적용되고 applyGlobalDisc()가 호출된다"
    - "#gDisc 값을 변경하면 임시 상태(red-lt 배경)가 표시되고, 저장 버튼을 누르지 않으면 DB 기본값은 변경되지 않는다"
    - "고객 선택 시 처방이력 건수가 드롭다운 배지에 표시된다"
    - "고객 저장 버튼이 폼 데이터를 CustomerDB.save()로 저장한다"
    - "고객 삭제가 인라인 2단계 확인 후 CustomerDB.delete()를 호출한다"
  artifacts:
    - path: "customerUI.js"
      provides: "Autocomplete, form fill, discount state, save/delete UI"
      contains: "injectCustomerCSS"
    - path: "index.html"
      provides: "customerUI.js script tag, autocomplete container, cust-actions row"
      contains: "customerUI.js"
  key_links:
    - from: "customerUI.js onCustomerSelect()"
      to: "#cName, #cPhone, #cAddr, #cArea, #cCrop, #gDisc"
      via: "document.getElementById().value = customer.*"
      pattern: "getElementById.*cName|getElementById.*gDisc"
    - from: "customerUI.js onCustomerSelect()"
      to: "uiController.js applyGlobalDisc()"
      via: "applyGlobalDisc() 호출"
      pattern: "applyGlobalDisc\\(\\)"
    - from: "customerUI.js onSaveCustomer()"
      to: "customerDB.js CustomerDB.save()"
      via: "CustomerDB.save(customer)"
      pattern: "CustomerDB\\.save"
    - from: "customerUI.js"
      to: "customerDB.js CustomerDB.search()"
      via: "CustomerDB.search(query)"
      pattern: "CustomerDB\\.search"
---

<objective>
customerUI.js를 신규 생성하여 자동완성 드롭다운, 폼 자동 채우기, 할인율 상태 관리, 고객 저장/삭제 UI를 구현하고 index.html에 연결한다.

Purpose: 고객 이름 입력만으로 기존 고객을 빠르게 불러오고, 할인율이 자동 적용되며, 고객 정보를 저장/관리할 수 있게 한다.
Output: customerUI.js (신규), index.html (수정)
</objective>

<execution_context>
@/Users/glen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-db/02-RESEARCH.md
@.planning/phases/02-db/02-UI-SPEC.md
@.planning/phases/02-db/02-01-SUMMARY.md
@customerDB.js
@index.html
@uiController.js

<interfaces>
<!-- CustomerDB API from customerDB.js (completed by Plan 02-01) -->
CustomerDB.save(customer)        -> Customer (with id, createdAt, updatedAt) or null
CustomerDB.delete(id)            -> void
CustomerDB.search(query)         -> Customer[] (name substring match)
CustomerDB.countPrescriptions(id) -> number
CustomerDB.list()                -> Customer[]
CustomerDB.findById(id)          -> Customer | null
CustomerDB.findByName(name)      -> Customer | null

<!-- Customer object shape -->
{ id: 'c_xxx', name, phone, addr, region, area, crop, discountRate (0-100 int), memo, createdAt, updatedAt }

<!-- Existing DOM IDs in .cust-bar -->
#cName (input), #cPhone (input), #cPhone2 (input), #cAddr (input),
#gDisc (input type=number), #cDate (input type=date), #cRegion (input),
#cArea (input type=number), #cAreaType (select), #cCrop (select)

<!-- Existing global function -->
applyGlobalDisc() — in index.html inline script (line ~1379), reads #gDisc.value and applies to cart items
syncPrint() — syncs form values to print preview
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: customerUI.js 생성 - 자동완성 + 폼 채우기 + 할인율 상태 + 저장/삭제</name>
  <files>customerUI.js</files>
  <read_first>customerDB.js, index.html (lines 538-595), uiController.js (injectValidationCSS pattern), .planning/phases/02-db/02-UI-SPEC.md, .planning/phases/02-db/02-RESEARCH.md</read_first>
  <action>
신규 파일 `customerUI.js`를 IIFE 패턴으로 생성한다. DOMContentLoaded 이벤트 안에서 초기화한다.

**1. CSS 주입 함수 `injectCustomerCSS()`:**
- `<style id="cust-css">` 생성, `document.head.appendChild()`
- Guard: `if (document.getElementById('cust-css')) return;`
- CSS 내용 (UI-SPEC 기반):
  - `#custAutocomplete`: `position:absolute; top:100%; left:0; width:100%; z-index:500; background:#fff; border:1.5px solid var(--border); border-radius:0 0 7px 7px; box-shadow:0 4px 12px rgba(0,0,0,.12); max-height:200px; overflow-y:auto; display:none;`
  - `.cust-ac-item`: `height:36px; padding:8px 11px; font-size:14px; font-weight:500; display:flex; align-items:center; justify-content:space-between; cursor:pointer;`
  - `.cust-ac-item:hover`: `background:var(--g-pale);`
  - `.cust-ac-item.active`: `background:var(--g-btn); color:#fff;`
  - `.cust-ac-badge`: `font-size:10px; color:var(--muted);`
  - `.cust-ac-empty`: `color:var(--muted); font-style:italic; pointer-events:none;`
  - `.cust-ac-match`: `font-weight:700;`
  - `.cust-actions`: `display:flex; gap:8px; margin-top:8px;`
  - `.cust-save-btn`: `flex:1; background:var(--g-btn); color:#fff; border:none; border-radius:7px; padding:10px; font-size:14px; font-weight:700; cursor:pointer;`
  - `.cust-save-btn:hover`: `background:var(--g-dark);`
  - `.cust-del-btn`: `min-width:72px; background:#fff0f0; color:var(--red); border:1.5px solid #FFCDD2; border-radius:5px; padding:8px 12px; font-size:14px; cursor:pointer;`
  - `.cust-del-confirm`: `display:flex; gap:4px;`
  - `.cust-del-yes`: `background:var(--red); color:#fff; border:none; border-radius:5px; padding:8px 12px; font-size:14px; cursor:pointer;`
  - `.cust-del-cancel`: `background:#fff; color:var(--muted); border:1.5px solid var(--border); border-radius:5px; padding:8px 10px; font-size:14px; cursor:pointer;`
  - `#discTempBadge`: `position:absolute; top:0; right:0; font-size:10px; font-weight:700; color:var(--red); background:var(--red-lt); padding:1px 4px; border-radius:3px; display:none;`
  - `#discSaveLink`: `display:none; font-size:14px; color:var(--g-btn); text-decoration:underline; cursor:pointer; margin-top:2px;`
  - `.cust-history-pill`: `font-size:10px; background:var(--g-pale); color:var(--g-mid); border-radius:9px; padding:1px 6px; margin-left:6px; display:none;`
  - `.cust-fill-flash`: `transition:background 0.4s ease;`
  - `#cName` autocomplete open state: `.cname-ac-open { border-color:var(--g-btn) !important; border-radius:7px 7px 0 0 !important; }`

**2. 클로저 변수:**
- `var _savedDiscount = 0;` — DB에 저장된 할인율
- `var _currentCustomerId = null;` — 현재 선택된 고객 ID
- `var _acIndex = -1;` — 자동완성 키보드 네비게이션 인덱스
- `var _deleteTimer = null;` — 삭제 확인 5초 자동 취소 타이머

**3. 자동완성 함수들 (CUST-02):**

`_onNameInput(e)`:
- `#cName`의 `keyup` 이벤트 리스너 (ArrowDown/ArrowUp/Enter/Escape는 별도 처리)
- `var query = document.getElementById('cName').value.trim();`
- `if (query.length < 1) { _closeAutocomplete(); return; }`
- `var results = CustomerDB.search(query);`
- `_renderAutocomplete(results, query);`

`_renderAutocomplete(results, query)`:
- `#custAutocomplete` 내부를 비우고 결과 렌더링
- 각 항목: `<div class="cust-ac-item" data-id="{customer.id}">` 안에 `<span class="cust-ac-name">{highlighted name}</span>` + `<span class="cust-ac-badge">{N}건</span>` (N > 0일 때만 표시)
- 이름 하이라이트: query 매칭 부분을 `<span class="cust-ac-match">{match}</span>`으로 감싸기
- 결과 없으면: `<div class="cust-ac-item cust-ac-empty">저장된 고객 없음</div>`
- `#custAutocomplete.style.display = 'block';`
- `#cName`에 `.cname-ac-open` 클래스 추가
- 처방이력 건수: `CustomerDB.countPrescriptions(customer.id)` 호출

`_onAcItemClick(e)`:
- 클릭된 `.cust-ac-item`의 `data-id`로 `CustomerDB.findById(id)` 호출
- `_onCustomerSelect(customer)` 호출

`_onNameKeydown(e)`:
- `ArrowDown`: _acIndex++, 해당 아이템에 `.active` 추가 (이전 것 제거)
- `ArrowUp`: _acIndex--, 해당 아이템에 `.active` 추가
- `Enter`: 현재 _acIndex의 아이템 선택 -> `_onCustomerSelect()`
- `Escape`: `_closeAutocomplete()`

`_closeAutocomplete()`:
- `#custAutocomplete.style.display = 'none';`
- `#cName`에서 `.cname-ac-open` 클래스 제거
- `_acIndex = -1;`

외부 클릭 닫기: `document.addEventListener('click', function(e) { ... })` — `#cName`과 `#custAutocomplete` 외부 클릭 시 150ms 딜레이로 닫기

**4. 고객 선택 및 폼 채우기 (CUST-02, CUST-03, DISC-01):**

`_onCustomerSelect(customer)`:
- `document.getElementById('cName').value = customer.name;`
- `document.getElementById('cPhone').value = customer.phone || '';`
- `document.getElementById('cAddr').value = customer.addr || '';`
- `document.getElementById('cRegion').value = customer.region || '';`
- `document.getElementById('cArea').value = customer.area || '';`
- 작물 (select): `var cropEl = document.getElementById('cCrop'); var opt = Array.from(cropEl.options).find(function(o) { return o.value === customer.crop; }); if (opt) cropEl.value = customer.crop;`
- `document.getElementById('gDisc').value = customer.discountRate || 0;`
- `_savedDiscount = customer.discountRate || 0;`
- `_currentCustomerId = customer.id;`
- `_updateDiscountState('saved');`
- `applyGlobalDisc();` — 기존 전역 함수 호출하여 장바구니에 즉시 반영
- `syncPrint();` — 인쇄 미리보기 동기화
- 처방이력 배지: `_updateHistoryBadge(customer.id);`
- 폼 필드 flash 효과: 각 필드에 잠시 `background:var(--g-pale)` 적용 후 400ms 후 제거 (`.cust-fill-flash` 활용)
- 저장 버튼 라벨: "고객 정보 수정"으로 변경
- 삭제 버튼: `display:''` (보이기)
- `_closeAutocomplete();`

**5. 할인율 상태 관리 (DISC-01, DISC-02):**

`_updateDiscountState(state)`:
- `var discEl = document.getElementById('gDisc');`
- `var badge = document.getElementById('discTempBadge');`
- `var saveLink = document.getElementById('discSaveLink');`
- `state === 'saved'`: discEl의 inline style 제거 (기존 `.fg.disc` CSS가 yellow 배경 처리), badge/saveLink 숨김
- `state === 'temp'`: `discEl.style.background = '#FFEBEE'; discEl.style.borderColor = '#EF9A9A';` badge/saveLink 보이기

`#gDisc`에 `addEventListener('input', ...)`:
- `if (!_currentCustomerId) return;` — 고객 미선택 시 무시
- `var current = Number(this.value) || 0;`
- `_updateDiscountState(current === _savedDiscount ? 'saved' : 'temp');`

`_onSaveDiscountAsDefault()`:
- `#discSaveLink` 클릭 시
- `var disc = Number(document.getElementById('gDisc').value) || 0;`
- `var customer = CustomerDB.findById(_currentCustomerId);`
- `if (customer) { customer.discountRate = disc; CustomerDB.save(customer); }`
- `_savedDiscount = disc;`
- `_updateDiscountState('saved');`

**6. 처방이력 배지 (CUST-05):**

`_updateHistoryBadge(customerId)`:
- `var count = CustomerDB.countPrescriptions(customerId);`
- `var pill = document.getElementById('historyPill');`
- `if (count > 0) { pill.textContent = count + '건 처방이력'; pill.style.display = 'inline'; }`
- `else { pill.style.display = 'none'; }`

**7. 고객 저장 버튼 (CUST-01 UI):**

`_onSaveCustomer()`:
- `var name = document.getElementById('cName').value.trim();`
- `if (!name) { alert('고객명을 입력하세요.'); return; }`
- customer 객체 구성: `{ id: _currentCustomerId || undefined, name, phone: #cPhone.value.trim(), addr: #cAddr.value.trim(), region: #cRegion.value.trim(), area: Number(#cArea.value) || 0, crop: #cCrop.value || '', discountRate: Number(#gDisc.value) || 0, memo: '' }`
- `var saved = CustomerDB.save(customer);`
- `if (!saved) return;`
- `_currentCustomerId = saved.id;`
- `_savedDiscount = saved.discountRate;`
- `_updateDiscountState('saved');`
- 버튼 라벨 -> "고객 정보 수정"
- 삭제 버튼 보이기
- 저장 성공 flash: 버튼 배경을 `#E0F5F5`로 300ms 후 원래로

**8. 고객 삭제 (인라인 2단계 확인):**

`_onDeleteCustomer()`:
- 삭제 버튼 클릭 시 -> 버튼 영역을 "정말 삭제?" + "예, 삭제" + "취소" 인라인 전환
- `_deleteTimer = setTimeout(_cancelDelete, 5000);` — 5초 후 자동 취소

`_confirmDelete()`:
- `CustomerDB.delete(_currentCustomerId);`
- `_currentCustomerId = null; _savedDiscount = 0;`
- `_updateDiscountState('saved');`
- 저장 버튼 라벨 -> "고객 저장"
- 삭제 버튼 숨기기
- `_cancelDelete();` — 인라인 확인 UI 복원

`_cancelDelete()`:
- `clearTimeout(_deleteTimer);`
- 삭제 버튼 영역을 원래 "고객 삭제" 버튼으로 복원

**9. 초기화 (`DOMContentLoaded`):**
- `injectCustomerCSS();`
- `#cName`의 `.fg` 부모에 `position:relative` 추가 (드롭다운 앵커)
- `#cName`에 keyup/keydown 이벤트 리스너 등록
- `#gDisc`에 input 이벤트 리스너 등록
- 저장/삭제 버튼에 click 이벤트 리스너 등록
- `#discSaveLink`에 click 이벤트 리스너 등록
  </action>
  <verify>
    <automated>grep -c "injectCustomerCSS\|_onCustomerSelect\|_updateDiscountState\|_onSaveCustomer\|_closeAutocomplete\|CustomerDB.search\|CustomerDB.save\|CustomerDB.delete\|CustomerDB.countPrescriptions\|DOMContentLoaded" customerUI.js</automated>
  </verify>
  <acceptance_criteria>
- `grep "injectCustomerCSS" customerUI.js` matches (CSS injection function)
- `grep "custAutocomplete" customerUI.js` matches (dropdown reference)
- `grep "CustomerDB.search" customerUI.js` matches (autocomplete search)
- `grep "cName" customerUI.js` matches (customer name field)
- `grep "cPhone" customerUI.js` matches (phone field)
- `grep "cAddr" customerUI.js` matches (address field)
- `grep "cArea" customerUI.js` matches (area field)
- `grep "cCrop" customerUI.js` matches (crop field)
- `grep "gDisc" customerUI.js` matches (discount field)
- `grep "_savedDiscount" customerUI.js` matches (discount state separation)
- `grep "applyGlobalDisc" customerUI.js` matches (existing discount function call)
- `grep "countPrescriptions" customerUI.js` matches (prescription history count)
- `grep "DOMContentLoaded" customerUI.js` matches (init timing)
- `grep "FFEBEE\|EF9A9A" customerUI.js` matches (temporary discount styles)
- `grep "cust-ac-item" customerUI.js` matches (dropdown item class)
  </acceptance_criteria>
  <done>customerUI.js contains autocomplete dropdown, form auto-fill, discount saved/temp state management, customer save/delete UI, all initialized on DOMContentLoaded.</done>
</task>

<task type="auto">
  <name>Task 2: index.html에 customerUI.js 연결 + DOM 마크업 추가</name>
  <files>index.html</files>
  <read_first>index.html (lines 538-600 for .cust-bar, lines 796-810 for script tags)</read_first>
  <action>
index.html에 네 가지 변경을 적용한다.

**변경 1: #cName의 .fg 부모에 position:relative + #custAutocomplete 삽입** (line 543 부근)

기존:
```html
<div class="fg"><label>고객명</label>
  <input id="cName" placeholder="홍길동" oninput="syncPrint()" onblur="checkUnpaidOnCustomer()"></div>
```

변경 후:
```html
<div class="fg" style="position:relative"><label>고객명</label>
  <input id="cName" placeholder="홍길동" oninput="syncPrint()" onblur="checkUnpaidOnCustomer()" autocomplete="off">
  <span id="historyPill" class="cust-history-pill" style="display:none"></span>
  <div id="custAutocomplete" style="display:none"></div>
</div>
```

- `.fg`에 `style="position:relative"` 추가 (드롭다운 절대위치 앵커)
- `#cName`에 `autocomplete="off"` 추가 (브라우저 자동완성 방지)
- `#historyPill` span 추가 (처방이력 건수 배지, #cName 옆)
- `#custAutocomplete` div 추가 (자동완성 드롭다운 컨테이너)

**변경 2: #gDisc의 .fg.disc 부모에 임시 할인율 배지/링크 삽입** (line 560 부근)

기존:
```html
<div class="fg disc"><label>할인율 (%)</label>
  <input id="gDisc" type="number" value="0" min="0" max="100" oninput="applyGlobalDisc()"></div>
```

변경 후:
```html
<div class="fg disc" style="position:relative"><label>할인율 (%)</label>
  <input id="gDisc" type="number" value="0" min="0" max="100" oninput="applyGlobalDisc()">
  <span id="discTempBadge" style="display:none">임시 변경 — 저장 안됨</span>
  <a id="discSaveLink" style="display:none">이 할인율을 기본값으로 저장</a>
</div>
```

- `.fg.disc`에 `style="position:relative"` 추가
- `#discTempBadge` span 추가
- `#discSaveLink` anchor 추가

**변경 3: .cust-bar 닫기 전에 .cust-actions 행 추가** (line 594, `</div><!-- .cust-bar -->` 직전)

`.cust-row3` 닫기(`</div>`) 뒤, `.cust-bar` 닫기(`</div>`) 전에:
```html
<div class="cust-actions">
  <button class="cust-save-btn" id="custSaveBtn">고객 저장</button>
  <button class="cust-del-btn" id="custDelBtn" style="display:none">고객 삭제</button>
</div>
```

**변경 4: script 태그 추가** (line 805, `customerDB.js` 바로 뒤)

```html
<script src="customerUI.js?v=1"></script>
```

`customerDB.js` 뒤에 위치해야 `CustomerDB` 전역 객체를 사용할 수 있다.
  </action>
  <verify>
    <automated>grep -c "custAutocomplete\|customerUI.js\|cust-actions\|custSaveBtn\|custDelBtn\|discTempBadge\|discSaveLink\|historyPill" index.html</automated>
  </verify>
  <acceptance_criteria>
- `grep "customerUI.js" index.html` matches (script tag)
- `grep "custAutocomplete" index.html` matches (dropdown container)
- `grep "cust-actions" index.html` matches (button row)
- `grep "custSaveBtn" index.html` matches (save button)
- `grep "custDelBtn" index.html` matches (delete button)
- `grep "discTempBadge" index.html` matches (temp discount badge)
- `grep "discSaveLink" index.html` matches (save as default link)
- `grep "historyPill" index.html` matches (history badge)
- `grep 'autocomplete="off"' index.html` matches (#cName browser autocomplete off)
- `grep "position:relative" index.html` matches (#cName .fg relative positioning)
- customerUI.js script tag is after customerDB.js
  </acceptance_criteria>
  <done>#custAutocomplete container, discount badge/link, save/delete buttons exist in index.html, and customerUI.js is loaded via script tag.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 브라우저에서 고객 DB 전체 흐름 검증</name>
  <files>index.html, customerUI.js</files>
  <action>
사용자가 브라우저에서 고객 저장, 자동완성, 할인율 상태, 삭제 흐름을 직접 확인한다.
  </action>
  <what-built>고객 자동완성, 폼 자동 채우기, 할인율 상태 관리, 고객 저장/삭제 전체 흐름</what-built>
  <how-to-verify>
1. 브라우저에서 index.html 열기
2. **고객 저장 테스트**: #cName에 "테스트농장" 입력, #cPhone에 "010-1234-5678", #cAddr에 "경기도", #cArea에 "400", #cCrop에서 작물 선택, #gDisc에 "10" 입력 -> "고객 저장" 클릭 -> 버튼이 "고객 정보 수정"으로 변경되는지 확인
3. **자동완성 테스트**: #cName을 지우고 "테스트" 입력 -> 드롭다운에 "테스트농장"이 나타나는지 확인 -> 클릭하여 선택 -> 모든 필드(이름/연락처/주소/면적/작물/할인율)가 채워지는지 확인
4. **할인율 상태 테스트**: #gDisc 값을 "15"로 변경 -> 배경이 빨간색 계열로 변하고 "임시 변경 -- 저장 안됨" 배지가 표시되는지 확인 -> 값을 다시 "10"으로 복원 -> 노란색 배경으로 돌아가는지 확인
5. **삭제 테스트**: "고객 삭제" 클릭 -> "정말 삭제?" + "예, 삭제" + "취소"가 인라인으로 나타나는지 확인 -> "취소" 클릭 -> 원래로 복원 확인
6. **키보드 테스트**: #cName에 글자 입력 후 ArrowDown/ArrowUp/Enter/Escape 동작 확인
  </how-to-verify>
  <verify>Manual browser verification</verify>
  <done>All customer DB UI flows work correctly in browser: autocomplete, form fill, discount states, save, delete.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `grep -c "customerUI.js" index.html` >= 1
- `grep -c "custAutocomplete" index.html` >= 1
- `grep -c "injectCustomerCSS\|_onCustomerSelect\|CustomerDB.search\|CustomerDB.save" customerUI.js` >= 4
- `grep -c "cust-actions" index.html` >= 1
- Browser: autocomplete dropdown, form fill, discount state, save/delete all working
</verification>

<success_criteria>
- #cName에 1자 이상 입력하면 CustomerDB.search() 결과가 드롭다운으로 표시된다
- 드롭다운 고객 선택 시 #cName, #cPhone, #cAddr, #cArea, #cCrop, #gDisc가 모두 채워진다
- 할인율이 저장된 값과 같으면 yellow, 다르면 red-lt 배경으로 표시된다
- "고객 저장" 클릭 시 CustomerDB.save()가 호출되고 성공 flash 표시된다
- "고객 삭제"가 인라인 2단계 확인을 거쳐 CustomerDB.delete()를 호출한다
- 처방이력 건수가 드롭다운 배지와 #cName 옆 pill로 표시된다
</success_criteria>

<output>
After completion, create `.planning/phases/02-db/02-02-SUMMARY.md`
</output>
