---
phase: 02-db
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - customerDB.js
autonomous: true
requirements:
  - CUST-01
  - CUST-04
  - CUST-05

must_haves:
  truths:
    - "CustomerDB.save({name:'테스트'}) returns object with id starting 'c_'"
    - "CustomerDB.save({id:'c_123', name:'변경'}) updates existing record"
    - "CustomerDB.delete('c_123') removes customer from localStorage"
    - "CustomerDB.search('김') returns array of matching customers"
    - "CustomerDB.countPrescriptions('c_123') returns integer count"
    - "discountRate stored as 0-100 integer, never decimal"
  artifacts:
    - path: "customerDB.js"
      provides: "Complete CRUD + search + countPrescriptions"
      exports: ["save", "delete", "search", "countPrescriptions"]
      contains: "save.*function"
  key_links:
    - from: "customerDB.js save()"
      to: "localStorage fertilizer_customers"
      via: "_set(KEYS.customers, list)"
      pattern: "_set\\(KEYS\\.customers"
    - from: "customerDB.js countPrescriptions()"
      to: "localStorage fertilizer_prescriptions"
      via: "_get(KEYS.prescriptions)"
      pattern: "_get\\(KEYS\\.prescriptions\\)"
---

<objective>
customerDB.js의 save/delete 스텁을 실제 CRUD로 구현하고, search() 및 countPrescriptions() 메서드를 추가한다.

Purpose: Wave 2의 customerUI.js가 의존하는 데이터 레이어를 완성한다.
Output: 완전한 CustomerDB API (save, delete, search, countPrescriptions)
</objective>

<execution_context>
@/Users/glen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-db/02-RESEARCH.md
@customerDB.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: save() 및 delete() 스텁을 실제 CRUD로 구현</name>
  <files>customerDB.js</files>
  <read_first>customerDB.js</read_first>
  <action>
customerDB.js의 IIFE 내부에서 두 스텁을 실제 구현으로 교체한다.

**save(customer) 구현** (line 51 부근):
- `customer.id`가 있으면 update: `_get(KEYS.customers)`에서 id 매칭 → `Object.assign({}, list[idx], customer, { updatedAt: now })` → `_set(KEYS.customers, list)` → return updated
- `customer.id`가 없거나 매칭 실패하면 create: `id: 'c_' + Date.now()`, `createdAt: now`, `updatedAt: now` 부여 → push → `_set(KEYS.customers, list)` → return newCustomer
- **discountRate 강제 정수화**: `customer.discountRate = Math.max(0, Math.min(100, Math.round(Number(customer.discountRate) || 0)))` — 소수, 음수, 100 초과 모두 차단
- **name 필수**: `if (!customer.name || !customer.name.trim()) return null;` — 이름 없으면 저장 거부
- `var now = new Date().toISOString();` 사용

**delete(id) 구현** (line 52 부근):
- `var list = _get(KEYS.customers);`
- `_set(KEYS.customers, list.filter(function(c) { return c.id !== id; }));`
- return void

기존 `list()`, `findById()`, `findByName()`, `exportAll()`, `importAll()` 은 수정하지 않는다.
  </action>
  <verify>
    <automated>grep -c "createdAt\|updatedAt\|c_.*Date.now\|Math.min(100\|list.filter" customerDB.js</automated>
  </verify>
  <acceptance_criteria>
- `grep "c_.*Date.now" customerDB.js` 매칭 (ID 생성 패턴)
- `grep "Math.min(100" customerDB.js` 매칭 (discountRate 0-100 강제)
- `grep "list.filter" customerDB.js` 매칭 (delete 구현)
- `grep "createdAt" customerDB.js` 매칭 (타임스탬프)
- save() 내부에 `customer.name` 검증 존재
- 기존 list/findById/findByName/exportAll/importAll 함수 미변경
  </acceptance_criteria>
  <done>CustomerDB.save()가 create/update를 수행하고 discountRate를 0-100 정수로 강제한다. CustomerDB.delete()가 id로 고객을 제거한다.</done>
</task>

<task type="auto">
  <name>Task 2: search() 및 countPrescriptions() 메서드 추가</name>
  <files>customerDB.js</files>
  <read_first>customerDB.js</read_first>
  <action>
customerDB.js의 `window.CustomerDB` 객체에 두 메서드를 추가한다. delete 다음, exportAll 이전 위치에 삽입.

**search(query) 추가:**
```javascript
search: function(query) {
  if (!query || !query.trim()) return [];
  var q = query.trim().toLowerCase();
  return _get(KEYS.customers).filter(function(c) {
    return c.name && c.name.toLowerCase().includes(q);
  });
},
```
- 1자 이상이면 검색 실행 (UI-SPEC: "after 1 character")
- 대소문자 무시, trim 처리
- 반환: Customer[] 배열

**countPrescriptions(customerId) 추가:**
```javascript
countPrescriptions: function(customerId) {
  if (!customerId) return 0;
  return _get(KEYS.prescriptions).filter(function(p) {
    return p.customerId === customerId;
  }).length;
},
```
- `fertilizer_prescriptions` 키에서 customerId 매칭하는 레코드 수 반환
- customerId가 없으면 0 반환

CUST-04 참고: `doExportJSON`/`doImportJSON`은 Phase 1에서 이미 index.html에 구현 완료 (lines 3171-3207). `CustomerDB.exportAll()`/`CustomerDB.importAll()`을 호출한다. Phase 2 추가 작업 없음.
  </action>
  <verify>
    <automated>grep -c "search.*function\|countPrescriptions.*function" customerDB.js</automated>
  </verify>
  <acceptance_criteria>
- `grep "search.*function" customerDB.js` 매칭 (search 메서드 존재)
- `grep "countPrescriptions.*function" customerDB.js` 매칭 (countPrescriptions 메서드 존재)
- `grep "KEYS.prescriptions" customerDB.js` 매칭 (처방이력 테이블 조회)
- `grep "toLowerCase" customerDB.js` 매칭 (대소문자 무시 검색)
- search()가 배열을 반환하는 구조
- countPrescriptions()가 정수를 반환하는 구조
  </acceptance_criteria>
  <done>CustomerDB.search('김')이 이름에 '김'이 포함된 고객 배열을 반환한다. CustomerDB.countPrescriptions('c_123')이 해당 고객의 처방이력 건수를 정수로 반환한다.</done>
</task>

</tasks>

<verification>
- `grep -c "save\|delete\|search\|countPrescriptions" customerDB.js` 결과 >= 8 (각 메서드 정의 + 내부 참조)
- `node -e "const fs=require('fs'); const c=fs.readFileSync('customerDB.js','utf8'); console.log(c.includes('Phase 2') ? 'STUBS GONE' : 'CHECK')"` — 스텁 주석이 실제 코드로 교체됨
- 기존 exportAll/importAll 함수 무변경 확인
</verification>

<success_criteria>
- CustomerDB.save()가 create/update 모두 지원, discountRate 0-100 정수 강제, 반환값에 id/createdAt/updatedAt 포함
- CustomerDB.delete(id)가 해당 고객 제거
- CustomerDB.search(query)가 이름 부분 매칭 배열 반환
- CustomerDB.countPrescriptions(customerId)가 정수 반환
- CUST-04는 Phase 1에서 완료 확인 (추가 작업 없음)
</success_criteria>

<output>
After completion, create `.planning/phases/02-db/02-01-SUMMARY.md`
</output>
