---
phase: 03-history
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - prescriptionHistoryUI.js
  - index.html
autonomous: false
requirements: [HIST-02, TMPL-01, TMPL-02]
must_haves:
  truths:
    - "처방이력 모달에서 과거 처방을 선택하면 장바구니와 고객 정보가 복원된다"
    - "처방이력 모달에서 고객명/작물/날짜로 필터링할 수 있다"
    - "작물 선택 시 해당 작물의 과거 처방이 있으면 템플릿 추천 배너가 나타난다"
    - "템플릿에서 불러온 수량이 면적 비율로 자동 조정된다"
    - "원본 스냅샷은 불러오기/수정 후에도 변경되지 않는다"
  artifacts:
    - path: "prescriptionHistoryUI.js"
      provides: "처방이력 모달 + 템플릿 배너 + 면적 비례 조정"
      contains: "prescriptionHistoryUI"
    - path: "index.html"
      provides: "히스토리 모달 HTML + 템플릿 배너 HTML + CSS"
      contains: "historyOverlay"
  key_links:
    - from: "prescriptionHistoryUI.js"
      to: "CustomerDB.listPrescriptions()"
      via: "function call in modal render"
      pattern: "CustomerDB\\.listPrescriptions"
    - from: "prescriptionHistoryUI.js"
      to: "CustomerDB.searchPrescriptions()"
      via: "function call in filter"
      pattern: "CustomerDB\\.searchPrescriptions"
    - from: "prescriptionHistoryUI.js"
      to: "CustomerDB.searchPrescriptionsByCrop()"
      via: "function call on cCrop change"
      pattern: "CustomerDB\\.searchPrescriptionsByCrop"
    - from: "prescriptionHistoryUI.js loadSnapshotIntoCart()"
      to: "cart (global), render(), applyGlobalDisc(), syncPrint()"
      via: "direct assignment and function calls"
      pattern: "cart\\s*="
    - from: "prescriptionHistoryUI.js scaleItemsByArea()"
      to: "cart items qty"
      via: "Math.max(1, Math.round(item.qty * ratio))"
      pattern: "Math\\.max\\(1"
---

<objective>
처방이력 UI 모달 + 템플릿 추천 배너 + 면적 비례 조정 구현

Purpose: HIST-02 (이력 불러오기), TMPL-01 (템플릿 추천), TMPL-02 (수량 비례 조정)을 완성하여 사용자가 과거 처방을 재활용할 수 있게 한다.
Output: prescriptionHistoryUI.js (신규), index.html에 모달 HTML + 배너 HTML + CSS 추가
</objective>

<execution_context>
@/Users/glen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-history/03-RESEARCH.md
@.planning/phases/03-history/03-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 추가된 API (customerDB.js) -->
From customerDB.js (Plan 01 output):
```javascript
window.CustomerDB = {
  // ... existing methods ...
  savePrescrSnapshot: function(snapshot) { /* returns snapshot or null */ },
  listPrescriptions: function(customerId) { /* returns sorted array */ },
  searchPrescriptions: function(opts) { /* opts: {name, crop, dateFrom, dateTo} → sorted array */ },
  searchPrescriptionsByCrop: function(crop) { /* returns sorted array */ },
  deletePrescription: function(id) { /* void */ }
};
```

<!-- Plan 01에서 추가된 getter (customerUI.js) -->
From customerUI.js (Plan 01 output):
```javascript
window.CustomerUI = {
  getCurrentCustomerId: function() { return _currentCustomerId; },
  refreshHistoryBadge: function(customerId) { /* updates historyPill badge */ }
};
```

<!-- index.html 기존 전역 변수/함수 -->
From index.html:
```javascript
let cart = [];                    // line 922
function render() { ... }        // line 1258 — re-renders ordList
function applyGlobalDisc() { ... } // line 1403 — applies discount to cart
function syncPrint() { ... }     // line 1445 — syncs print preview
function showToast(msg) { ... }  // exists in index.html
```

<!-- DOM 구조 참조 -->
From index.html DOM:
```html
<!-- line 545: historyPill (배지 — 클릭 이벤트 연결 필요) -->
<span id="historyPill" class="cust-history-pill" style="display:none"></span>

<!-- line 589: cCrop select (change 이벤트 필요) -->
<select id="cCrop">...</select>

<!-- line 641: ordList (템플릿 배너를 이 위에 삽입) -->
<div class="ord-list" id="ordList">

<!-- line 828-829: script 순서 -->
<script src="customerDB.js?v=2"></script>
<script src="customerUI.js?v=1"></script>
<script src="prescriptionHistoryUI.js?v=1"></script>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: prescriptionHistoryUI.js 생성 — 모달 + 필터 + 불러오기 + 템플릿 배너</name>
  <files>prescriptionHistoryUI.js</files>
  <read_first>
    - customerUI.js (IIFE 패턴, CSS 주입 방식, overlay 모달 패턴 참조 — custListOverlay 400-445행)
    - customerDB.js (Plan 01에서 추가된 API 확인)
    - index.html (ordList 641행, historyPill 545행, cCrop 589행, cart/render/applyGlobalDisc/syncPrint 위치)
    - .planning/phases/03-history/03-01-SUMMARY.md (Plan 01 결과 확인)
  </read_first>
  <action>
**신규 파일 `prescriptionHistoryUI.js` 생성 — IIFE 패턴, window.PrescriptionHistoryUI 네임스페이스:**

전체 구조:
```
(function() {
  'use strict';
  // 1. CSS 주입
  // 2. 모달 HTML 생성
  // 3. 모달 렌더링/필터
  // 4. 스냅샷 불러오기 (loadSnapshotIntoCart)
  // 5. 면적 비례 조정 (scaleItemsByArea)
  // 6. 템플릿 배너 (crop change 이벤트)
  // 7. DOMContentLoaded 초기화
  // 8. window.PrescriptionHistoryUI 공개 API
})();
```

**1. CSS 주입 함수 `_injectHistoryCSS()`:**
- `<style id="hist-css">` 생성하여 `document.head`에 추가
- 스타일 정의:
  - `#historyOverlay` — `custListOverlay`와 동일 패턴: `position:fixed; inset:0; background:rgba(0,0,0,.45); z-index:900; display:none; align-items:flex-start; justify-content:center; padding-top:60px;`
  - `#historyOverlay.on { display:flex; }`
  - `#historyPanel` — `#custListPanel`과 동일: `background:#fff; border-radius:14px; width:min(720px,95vw); max-height:75vh; display:flex; flex-direction:column; overflow:hidden; box-shadow:0 8px 32px rgba(0,0,0,.2);`
  - `.hist-head` — flex row, padding 16px 20px, border-bottom
  - `.hist-title` — font-size:17px, font-weight:700
  - `.hist-close` — background:none, border:none, font-size:22px, cursor:pointer
  - `.hist-filters` — padding:12px 20px, display:flex, gap:10px, flex-wrap:wrap, border-bottom:1.5px solid var(--border)
  - `.hist-filter-input` — flex:1, min-width:120px, padding:8px 12px, border:1.5px solid var(--border), border-radius:7px, font-size:13px
  - `.hist-body` — overflow-y:auto, flex:1
  - `.hist-item` — padding:14px 18px, border-bottom:1px solid var(--border), cursor:pointer
  - `.hist-item:hover { background:var(--g-pale); }`
  - `.hist-item-head` — display:flex, justify-content:space-between, align-items:center
  - `.hist-item-name` — font-size:15px, font-weight:700
  - `.hist-item-date` — font-size:12px, color:var(--muted)
  - `.hist-item-meta` — font-size:13px, color:var(--muted), margin-top:4px
  - `.hist-item-total` — font-size:14px, font-weight:600, color:var(--g-btn), margin-top:4px
  - `.hist-empty` — padding:40px, text-align:center, color:var(--muted), font-size:15px
  - `#tmplBanner` — background:var(--g-pale), border:1.5px solid var(--g-btn), border-radius:10px, padding:12px 16px, margin-bottom:10px, display:none
  - `.tmpl-banner-text` — font-size:13px, color:#333, margin-bottom:8px
  - `.tmpl-banner-actions` — display:flex, gap:8px
  - `.tmpl-use-btn` — background:var(--g-btn), color:#fff, border:none, border-radius:7px, padding:8px 16px, font-size:13px, font-weight:700, cursor:pointer
  - `.tmpl-skip-btn` — background:none, border:1.5px solid var(--border), border-radius:7px, padding:8px 12px, font-size:13px, color:var(--muted), cursor:pointer

**2. 모달 HTML 생성 함수 `_createHistoryModal()`:**
- `document.createElement('div')` → id="historyOverlay"
- innerHTML:
```html
<div id="historyPanel">
  <div class="hist-head">
    <span class="hist-title">처방이력</span>
    <button class="hist-close" id="histClose">&times;</button>
  </div>
  <div class="hist-filters">
    <input class="hist-filter-input" id="histFilterName" placeholder="고객명">
    <select class="hist-filter-input" id="histFilterCrop">
      <option value="">전체 작물</option>
    </select>
    <input class="hist-filter-input" id="histFilterFrom" type="date" title="시작일">
    <input class="hist-filter-input" id="histFilterTo" type="date" title="종료일">
  </div>
  <div class="hist-body" id="histBody"></div>
</div>
```
- `document.body.appendChild(overlay)`

**3. 작물 옵션 동적 생성 `_populateCropFilter()`:**
- `#cCrop`의 options를 읽어서 `#histFilterCrop`에 복사 (value + textContent)
- 첫 번째 옵션은 "전체 작물" (빈 value)

**4. 모달 열기/닫기:**
- `_openHistoryModal(filterCrop)` — overlay에 'on' class 추가, filterCrop 있으면 histFilterCrop에 설정, `_renderHistoryList()` 호출
- `_closeHistoryModal()` — overlay에서 'on' class 제거

**5. 이력 목록 렌더링 `_renderHistoryList()`:**
- 필터 값 수집: histFilterName.value, histFilterCrop.value, histFilterFrom.value, histFilterTo.value
- `CustomerDB.searchPrescriptions({ name: name, crop: crop, dateFrom: from, dateTo: to })` 호출
- 결과가 없으면: `<div class="hist-empty">처방이력이 없습니다.</div>`
- 결과 있으면 각 항목 렌더링:
  ```html
  <div class="hist-item" data-id="{p.id}">
    <div class="hist-item-head">
      <span class="hist-item-name">{p.customer.name}</span>
      <span class="hist-item-date">{p.savedAt를 YYYY.MM.DD로 포맷}</span>
    </div>
    <div class="hist-item-meta">{p.customer.crop} · {p.customer.area}평 · {p.items.length}개 품목</div>
    <div class="hist-item-total">합계: {p.totals.grandTotal.toLocaleString()}원</div>
  </div>
  ```
- 각 `.hist-item`에 click 이벤트: `_onHistoryItemClick(p)` 호출

**6. 이력 항목 클릭 `_onHistoryItemClick(snapshot)`:**
- `_loadSnapshotIntoCart(snapshot)` 호출
- `_closeHistoryModal()` 호출

**7. 스냅샷 불러오기 `_loadSnapshotIntoCart(snapshot)` — HIST-02 핵심:**
- **원본 보호:** snapshot 객체를 직접 수정하지 않음. `JSON.parse(JSON.stringify(snapshot))`로 deep copy 사용
- cart 교체:
  ```javascript
  cart = copy.items.map(function(item, idx) {
    return {
      i: 'hist_' + Date.now() + '_' + idx,
      name:  item.name,
      size:  item.size,
      retail: item.retail,
      sp:    item.sp,
      qty:   item.qty,
      disc:  item.retail > 0 ? Math.round((1 - item.sp / item.retail) * 100) : 0,
      gift:  item.gift || false,
      custom: true
    };
  });
  ```
- 폼 필드 채우기:
  - `document.getElementById('cName').value = copy.customer.name || ''`
  - `document.getElementById('cCrop').value = copy.customer.crop || ''` (select이므로 value 직접 할당)
  - `document.getElementById('cArea').value = copy.customer.area || ''`
  - `document.getElementById('cRegion').value = copy.customer.region || ''`
  - `document.getElementById('gDisc').value = copy.discountRate || 0`
- 전역 함수 호출 (존재 확인 후):
  - `if (typeof applyGlobalDisc === 'function') applyGlobalDisc();`
  - `if (typeof render === 'function') render();`
  - `if (typeof syncPrint === 'function') syncPrint();`
- 토스트: `if (typeof showToast === 'function') showToast('처방이력 불러옴 — 수정 후 재발행 가능');`

**8. 면적 비례 조정 `_scaleItemsByArea(items, sourceArea, newArea)` — TMPL-02:**
- Guard: `if (!sourceArea || sourceArea <= 0 || !newArea || newArea <= 0) return items;`
- `var ratio = newArea / sourceArea;`
- `return items.map(function(item) { return Object.assign({}, item, { qty: Math.max(1, Math.round(item.qty * ratio)) }); });`

**9. 템플릿 배너 `_showTmplBanner(snapshot)` / `_hideTmplBanner()` — TMPL-01:**
- `_showTmplBanner(snapshot)`:
  - `#tmplBanner` 요소의 display를 'block'으로
  - 배너 텍스트: `'{snapshot.customer.name}님의 {snapshot.customer.crop} 처방 ({new Date(snapshot.savedAt).toLocaleDateString('ko-KR')}) — {snapshot.items.length}개 품목'`
  - "이 처방 사용" 버튼 클릭 시: `_onUseTmpl(snapshot)`
  - "건너뛰기" 버튼 클릭 시: `_hideTmplBanner()`
  - "다른 이력 보기" 링크 클릭 시: `_openHistoryModal(snapshot.customer.crop)`
- `_hideTmplBanner()`: `#tmplBanner.style.display = 'none'`

**10. 템플릿 사용 `_onUseTmpl(snapshot)`:**
- 현재 면적: `parseFloat((document.getElementById('cArea') || {}).value) || 0`
- 소스 면적: `snapshot.customer.area || 0`
- `var scaledItems = _scaleItemsByArea(snapshot.items, sourceArea, currentArea);`
- 면적 없으면 스케일링 생략, 토스트: `'면적 미입력 — 수량 비례 조정 건너뜀'`
- cart에 로드 (loadSnapshotIntoCart와 유사하되, items를 scaledItems로 대체)
- `_hideTmplBanner()`

**11. cCrop change 이벤트 `_onCropChange()`:**
- `var crop = (document.getElementById('cCrop') || {}).value || '';`
- `if (!crop) { _hideTmplBanner(); return; }`
- `var matches = CustomerDB.searchPrescriptionsByCrop(crop);`
- `if (matches.length > 0) { _showTmplBanner(matches[0]); } else { _hideTmplBanner(); }`

**12. DOMContentLoaded 초기화:**
- `_injectHistoryCSS()`
- `_createHistoryModal()`
- 템플릿 배너 HTML 삽입: `#ordList` 바로 앞에 `<div id="tmplBanner" style="display:none">` 추가
  - innerHTML: `<div class="tmpl-banner-text" id="tmplBannerText"></div><div class="tmpl-banner-actions"><button class="tmpl-use-btn" id="tmplUseBtn">이 처방 사용</button><a href="#" id="tmplOtherLink" style="font-size:13px;color:var(--g-btn);align-self:center;">다른 이력 보기</a><button class="tmpl-skip-btn" id="tmplSkipBtn">건너뛰기</button></div>`
  - `ordList.parentElement.insertBefore(tmplBanner, ordList)`
- 이벤트 연결:
  - `#histClose` click → `_closeHistoryModal()`
  - `#historyOverlay` click (self only) → `_closeHistoryModal()`
  - `#histFilterName` input → debounce 200ms → `_renderHistoryList()`
  - `#histFilterCrop` change → `_renderHistoryList()`
  - `#histFilterFrom` change → `_renderHistoryList()`
  - `#histFilterTo` change → `_renderHistoryList()`
  - `#historyPill` click → `_openHistoryModal()` (현재 고객의 이력으로)
  - `#cCrop` addEventListener('change', `_onCropChange`) — `change` 이벤트 사용 (select 요소이므로 input 아님)

**13. 공개 API:**
```javascript
window.PrescriptionHistoryUI = {
  openModal: _openHistoryModal,
  closeModal: _closeHistoryModal
};
```
  </action>
  <verify>
    <automated>cd /Users/glen/천연비료처방전/천연비료처방전 && test -f prescriptionHistoryUI.js && grep -c "historyOverlay" prescriptionHistoryUI.js && grep -c "loadSnapshotIntoCart\|_loadSnapshotIntoCart" prescriptionHistoryUI.js && grep -c "scaleItemsByArea\|_scaleItemsByArea" prescriptionHistoryUI.js && grep -c "tmplBanner" prescriptionHistoryUI.js && grep -c "searchPrescriptionsByCrop" prescriptionHistoryUI.js && grep -c "Math.max(1" prescriptionHistoryUI.js</automated>
  </verify>
  <acceptance_criteria>
    - prescriptionHistoryUI.js 파일이 존재하고 IIFE 패턴으로 래핑되어 있다: `(function()` 로 시작
    - `window.PrescriptionHistoryUI` 객체가 정의되어 있다
    - `_loadSnapshotIntoCart` 함수 안에 `JSON.parse(JSON.stringify(snapshot))` deep copy가 존재한다
    - `_loadSnapshotIntoCart` 함수 안에 `cart =` 할당이 존재한다
    - `_loadSnapshotIntoCart` 함수 안에 `applyGlobalDisc` 호출이 존재한다
    - `_loadSnapshotIntoCart` 함수 안에 `render()` 호출이 존재한다
    - `_loadSnapshotIntoCart` 함수 안에 `syncPrint()` 호출이 존재한다
    - `_scaleItemsByArea` 함수에 `Math.max(1, Math.round(item.qty * ratio))` 패턴이 존재한다
    - `_scaleItemsByArea` 함수에 `sourceArea <= 0` guard가 존재한다
    - `_onCropChange` 함수에서 `CustomerDB.searchPrescriptionsByCrop(crop)` 호출이 존재한다
    - `#cCrop`에 `change` 이벤트를 연결한다 (input이 아님): `addEventListener('change'`
    - `CustomerDB.searchPrescriptions` 호출이 존재한다 (필터 기능)
    - `#historyOverlay` 모달 HTML이 생성된다
    - `#tmplBanner` HTML이 `#ordList` 앞에 삽입된다: `insertBefore`
    - `#historyPill` click 이벤트가 연결된다
    - `showToast('처방이력 불러옴` 문자열이 존재한다
    - 필터 debounce를 위한 `setTimeout` 패턴이 존재한다
  </acceptance_criteria>
  <done>
    prescriptionHistoryUI.js 파일이 생성되고 모든 기능이 동작한다:
    - historyPill 클릭 시 처방이력 모달이 열린다
    - 모달에서 고객명/작물/날짜 필터가 작동한다
    - 이력 항목 클릭 시 장바구니와 폼이 복원된다 (원본 불변)
    - cCrop 변경 시 템플릿 배너가 나타난다
    - "이 처방 사용" 클릭 시 면적 비례 조정된 항목이 장바구니에 로드된다
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 처방이력 + 템플릿 기능 브라우저 검증</name>
  <files>prescriptionHistoryUI.js, index.html, customerDB.js, customerUI.js</files>
  <action>
사용자에게 브라우저 검증을 요청한다. 처방이력 저장/조회/불러오기 + 템플릿 추천 배너 + 면적 비례 조정 전체 기능을 확인해야 한다.

검증 단계:
1. index.html을 브라우저에서 연다
2. HIST-01 검증 (자동 저장): 장바구니에 제품 추가 후 고객명/연락처 입력 후 인쇄 버튼 클릭 후 콘솔에서 JSON.parse(localStorage.getItem('fertilizer_prescriptions')) 확인하여 1건 이상
3. HIST-03 검증 (필터): historyPill 배지 클릭하여 모달 열림 확인, 고객명으로 검색하여 결과 필터링 확인
4. HIST-02 검증 (불러오기): 모달에서 이력 항목 클릭하여 장바구니와 폼 필드에 데이터 복원 확인, 콘솔에서 원본 스냅샷 변경 없음 확인
5. TMPL-01 검증 (템플릿 배너): 새 고객 정보 입력 후 작물을 저장된 이력과 같은 작물로 변경하여 ordList 위에 템플릿 추천 배너 나타남 확인
6. TMPL-02 검증 (비례 조정): 면적을 다르게 입력 후 이 처방 사용 클릭하여 수량이 면적 비율로 조정됨 확인 (예: 원래 100평에서 3개이면 200평에서 6개)
7. 이력이 없는 작물 선택 시 배너가 나타나지 않음 확인

resume-signal: "approved" 또는 수정사항 기술
  </action>
  <verify>사용자가 위 7개 검증 단계를 모두 통과했다고 확인</verify>
  <done>사용자가 "approved"로 응답하여 Phase 03 전체 기능이 검증됨</done>
</task>

</tasks>

<verification>
1. prescriptionHistoryUI.js 파일이 존재하고 IIFE로 래핑되어 있다
2. 브라우저에서 에러 없이 로드된다 (콘솔에 에러 없음)
3. historyPill 클릭 시 모달이 열린다
4. 모달 필터(고객명, 작물, 날짜)가 동작한다
5. 이력 항목 클릭 시 장바구니가 복원된다
6. cCrop 변경 시 템플릿 배너가 나타난다
7. 템플릿 불러오기 시 수량 비례 조정이 동작한다
8. 원본 스냅샷이 변경되지 않는다
</verification>

<success_criteria>
- 처방이력 모달에서 과거 처방을 조회하고 필터링할 수 있다
- 이력 항목을 클릭하면 장바구니와 폼이 복원되며, 원본은 불변이다
- 작물 선택 시 과거 처방이 있으면 템플릿 배너가 나타난다
- 템플릿 사용 시 면적 비례 조정이 적용된다
- 사용자가 모든 기능을 브라우저에서 확인하고 승인한다
</success_criteria>

<output>
After completion, create `.planning/phases/03-history/03-02-SUMMARY.md`
</output>
