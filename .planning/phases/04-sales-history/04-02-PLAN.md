---
phase: 04-sales-history
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - salesHistoryUI.js
  - index.html
autonomous: false
requirements: [SALE-04, SALE-05]

must_haves:
  truths:
    - "매출 집계 버튼을 클릭하면 기간별(이번 달/지난 달/직접 입력) 매출 합계가 오버레이에 표시된다"
    - "매출 집계 오버레이의 미수금 숫자 클릭 시 전체 미수금 목록 오버레이가 열린다"
    - "고객 선택 시 해당 고객의 총 거래금액과 미수금 잔액이 인라인 배지로 표시된다"
    - "거래가 없는 고객은 요약 배지가 숨겨진다"
    - "전체 미수금 목록에서 지역별 그룹핑 + 지역별 소계가 표시된다"
  artifacts:
    - path: "salesHistoryUI.js"
      provides: "매출 집계 오버레이(기간별 + salesUnpaidLink) + 고객별 요약 배지"
      exports: ["SalesHistoryUI.openSalesPanel", "SalesHistoryUI.updateCustomerSummary"]
    - path: "index.html"
      provides: "openAllUnpaid 지역별 그룹 + salesHistoryUI.js script 연결 + 매출 집계 버튼"
      contains: "salesHistoryUI.js"
  key_links:
    - from: "salesHistoryUI.js _renderSalesStats()"
      to: "openAllUnpaid()"
      via: "salesUnpaidLink onclick"
      pattern: "salesUnpaidLink"
    - from: "salesHistoryUI.js _calcPeriodSales()"
      to: "getInvoiceHistory()"
      via: "전역 함수 호출"
      pattern: "getInvoiceHistory"
    - from: "index.html openAllUnpaid()"
      to: "byRegion 지역별 그룹"
      via: "지역별 카드 렌더링"
      pattern: "byRegion"
---

<objective>
매출 집계 UI + 미수금→매출 링크 + 지역별 미수금 검증 + 브라우저 체크포인트

Purpose: SALE-04/05 UI를 완성하고, 추가 요구사항(매출→미수금 링크, 지역별 묶음)을 포함한 전체 Phase 4 동작을 브라우저에서 검증한다.
Output: salesHistoryUI.js salesUnpaidLink 검증/보강, index.html 연결 상태 확인, 브라우저 검증
</objective>

<execution_context>
@/Users/glen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sales-history/04-RESEARCH.md
@.planning/phases/04-sales-history/04-01-PLAN.md

<interfaces>
<!-- Interfaces from Plan 01 and existing implementations -->

From salesHistoryUI.js (현재 상태 — 이미 구현 완료):
```javascript
// _renderSalesStats() 내 salesUnpaidLink 처리 — 이미 구현됨
var unpaidLink = document.getElementById('salesUnpaidLink');
if (unpaidLink) {
  if (result.unpaid > 0) {
    unpaidLink.style.display = 'block';
    unpaidLink.onclick = function(e) {
      e.preventDefault();
      _closeSalesPanel();
      if (typeof openAllUnpaid === 'function') openAllUnpaid();
    };
  } else {
    unpaidLink.style.display = 'none';
  }
}

// salesUnpaidLink HTML — 오버레이 내 미수금 stat-item 안에 있어야 함
// '<a id="salesUnpaidLink" href="#" style="display:none;font-size:11px;color:#C62828;
//   text-decoration:underline;margin-top:2px;">전체 목록 보기 →</a>'

// window.SalesHistoryUI 노출
window.SalesHistoryUI = {
  openSalesPanel: openSalesPanel,
  updateCustomerSummary: _updateCustomerSummary
};
```

From index.html (현재 상태 — 이미 구현 완료):
```javascript
// script 태그 — main.js 뒤에 있음
// <script src="salesHistoryUI.js?v=1"></script>

// 매출 집계 버튼
// <button class="btn-all-unpaid" onclick="SalesHistoryUI.openSalesPanel()"
//   style="background:#E3F2FD;color:#1565C0;border-color:#1565C0;">매출 집계</button>

// openAllUnpaid() — 지역별 그룹 + 소계 이미 구현됨
// byRegion 객체, regions.forEach(), regionTotal 계산
```

From customerDB.js (Plan 01에서 완성):
```javascript
CustomerDB.getCustomerSummary(name, phone)
// 반환: { count, totalAmount, totalPaid, unpaid, invoices }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: salesHistoryUI.js salesUnpaidLink + openAllUnpaid 연결 검증 및 보강</name>
  <files>salesHistoryUI.js</files>
  <action>
salesHistoryUI.js의 현재 구현을 읽고 다음 항목을 검증한다. 누락된 항목만 추가하고 이미 존재하는 코드는 건드리지 않는다.

**검증 체크리스트:**

1. `_createSalesOverlay()` 내 salesOverlay HTML에 `salesUnpaidLink` 앵커 태그가 있는지 확인:
   - 있어야 할 위치: 미수금 `.sales-stat-item` 내부, `salesUnpaid` div 아래
   - 있으면 OK, 없으면 아래 코드를 `id="salesUnpaid"` div 바로 뒤에 추가:
   ```html
   <a id="salesUnpaidLink" href="#" style="display:none;font-size:11px;color:#C62828;text-decoration:underline;margin-top:2px;">전체 목록 보기 →</a>
   ```

2. `_renderSalesStats()` 내 `salesUnpaidLink` 클릭 이벤트 처리 코드가 있는지 확인:
   - 있으면 OK
   - 없으면 `document.getElementById('salesUnpaid').textContent = ...` 줄 이후에 추가:
   ```javascript
   var unpaidLink = document.getElementById('salesUnpaidLink');
   if (unpaidLink) {
     if (result.unpaid > 0) {
       unpaidLink.style.display = 'block';
       unpaidLink.onclick = function(e) {
         e.preventDefault();
         _closeSalesPanel();
         if (typeof openAllUnpaid === 'function') openAllUnpaid();
       };
     } else {
       unpaidLink.style.display = 'none';
     }
   }
   ```

3. `window.SalesHistoryUI` 노출 객체에 `openSalesPanel`과 `updateCustomerSummary` 가 있는지 확인.

모든 항목이 이미 있으면 파일을 변경하지 않고 검증만 한다.
  </action>
  <verify>
    <automated>grep -q "salesUnpaidLink" /Users/glen/천연비료처방전/천연비료처방전/salesHistoryUI.js && grep -q "openAllUnpaid" /Users/glen/천연비료처방전/천연비료처방전/salesHistoryUI.js && echo "PASS: salesUnpaidLink and openAllUnpaid link present" || echo "FAIL: missing salesUnpaidLink or openAllUnpaid"</automated>
  </verify>
  <done>salesHistoryUI.js에 salesUnpaidLink와 openAllUnpaid() 연결이 존재하며, 미수금 > 0일 때 "전체 목록 보기" 링크가 표시된다</done>
</task>

<task type="auto">
  <name>Task 2: index.html script 로드 순서 + 매출 집계 버튼 검증</name>
  <files>index.html</files>
  <action>
index.html을 읽고 다음 항목을 검증한다. 누락된 항목만 추가한다.

**검증 체크리스트:**

1. `<script src="salesHistoryUI.js` 태그가 있는지 확인 (버전 파라미터 포함 여부 무관).
   - 없으면: `<script src="main.js">` 또는 `</body>` 앞에 추가:
   ```html
   <script src="salesHistoryUI.js?v=1"></script>
   ```
   - 있으면: 현재 v 버전을 확인. 이 플랜에서 salesHistoryUI.js를 수정했으면 `?v=2`로 올린다.

2. `SalesHistoryUI.openSalesPanel` 버튼이 있는지 확인.
   - 없으면: `openAllUnpaid()` 버튼 바로 다음에 추가:
   ```html
   <button class="btn-all-unpaid" onclick="SalesHistoryUI.openSalesPanel()" style="background:#E3F2FD;color:#1565C0;border-color:#1565C0;">매출 집계</button>
   ```

3. script 로드 순서 확인: `customerDB.js` → `customerUI.js` → `salesHistoryUI.js` 순서이어야 한다. 순서가 잘못되어 있으면 재정렬.

모든 항목이 이미 있으면 파일을 변경하지 않는다.
  </action>
  <verify>
    <automated>grep -q "salesHistoryUI.js" /Users/glen/천연비료처방전/천연비료처방전/index.html && grep -q "SalesHistoryUI.openSalesPanel" /Users/glen/천연비료처방전/천연비료처방전/index.html && echo "PASS" || echo "FAIL: script or button missing"</automated>
  </verify>
  <done>index.html에 salesHistoryUI.js 로드 + 매출 집계 버튼이 있고, 스크립트 로드 순서가 올바르다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 브라우저 검증 — Phase 4 전체 기능</name>
  <files>none</files>
  <action>Human verification checkpoint. No automated changes. Verify all Phase 4 features work correctly in the browser after Tasks 1 and 2 complete.</action>
  <what-built>
Phase 4 전체 기능:
- SALE-01~03: 거래 저장 + 납부 기록 + 미수금 목록 (기존 구현)
- SALE-04: 매출 집계 오버레이(이번달/지난달/직접입력) + 미수금 → 전체 목록 링크
- SALE-05: 고객 선택 시 거래요약 배지(거래 N건 | 총 X원 | 미수금 Y원)
- 추가: 자동완성에 처방이력 고객 표시("이력" 배지) + 배지 건수 수정
- 추가: 전체 미수금 목록 지역별 그룹 + 지역 소계
- 추가: openAllUnpaid 소프트 삭제 항목 제외 (h.status==='삭제됨')
  </what-built>
  <how-to-verify>
1. 브라우저에서 `index.html`을 연다 (파일을 직접 열거나 로컬 서버 사용).

2. **SALE-01/02 검증 — 거래 저장 + 미러 쓰기:**
   - 고객 이름/전화를 입력하고 상품 추가 후 발행(인쇄) 버튼을 클릭한다
   - 콘솔에서 `JSON.parse(localStorage.getItem('fertilizer_transactions'))` 확인 — 방금 저장한 레코드가 있어야 한다
   - 레코드에 `customerId` 필드가 있는지 확인 (null도 허용)

3. **SALE-03 검증 — 미수금 목록 + 지역별 그룹:**
   - "미수금 전체 목록" 버튼을 클릭한다
   - 지역별로 카드가 그룹핑되어 표시되는지 확인
   - 각 지역 카드에 건수와 소계 금액이 표시되는지 확인 (예: "3건 · 1,200,000원")
   - 삭제된 거래가 미수금 목록에 표시되지 않는지 확인

4. **SALE-04 검증 — 매출 집계:**
   - "매출 집계" 버튼을 클릭한다 — 오버레이가 열려야 한다
   - "이번 달" 버튼이 기본 선택 상태로 거래 건수/총 매출/입금액/미수금이 표시되는지 확인
   - "지난 달" 버튼을 클릭 — 지난 달 통계로 갱신되는지 확인
   - "직접 입력"을 선택하고 날짜 범위 입력 후 "조회" 버튼 클릭 — 해당 기간 통계 표시 확인
   - 미수금 > 0이면 "전체 목록 보기 →" 링크가 표시되는지 확인
   - 그 링크를 클릭하면 매출 오버레이가 닫히고 전체 미수금 목록이 열리는지 확인

5. **SALE-05 검증 — 고객별 요약 배지:**
   - 고객 이름 자동완성으로 기존 고객을 선택한다
   - 고객 폼 영역에 "거래 N건 | 총 X원 | 미수금 Y원" 배지가 표시되는지 확인
   - 미수금이 있는 고객의 배지가 빨간 계열 색상인지 확인
   - 거래 이력이 없는 고객 선택 시 배지가 숨겨지는지 확인

6. **추가 요구사항 — 이력 자동완성:**
   - CustomerDB에 없지만 처방이력에 있는 고객 이름을 입력한다
   - 자동완성 드롭다운에 "이력" 배지와 함께 표시되는지 확인
   - 선택 시 폼이 채워지고 "처방이력에서 불러왔습니다" 토스트가 표시되는지 확인
   - 자동완성 드롭다운의 처방이력 건수가 실제 이력 수와 일치하는지 확인
  </how-to-verify>
  <verify>
    <automated>MISSING — human browser verification required for this checkpoint</automated>
  </verify>
  <done>사용자가 "approved"를 입력하거나 발견된 문제를 설명한다</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. `grep "salesUnpaidLink" /Users/glen/천연비료처방전/천연비료처방전/salesHistoryUI.js` — 미수금 링크 존재
2. `grep "openAllUnpaid" /Users/glen/천연비료처방전/천연비료처방전/salesHistoryUI.js` — 링크 연결
3. `grep "salesHistoryUI.js" /Users/glen/천연비료처방전/천연비료처방전/index.html` — script 로드
4. `grep "SalesHistoryUI.openSalesPanel" /Users/glen/천연비료처방전/천연비료처방전/index.html` — 매출 버튼
5. `grep "h\.status.*삭제됨" /Users/glen/천연비료처방전/천연비료처방전/index.html` — openAllUnpaid 버그 수정 (Plan 01)
6. `grep "searchPrescriptions" /Users/glen/천연비료처방전/천연비료처방전/customerUI.js` — 배지 버그 수정 (Plan 01)
7. 브라우저 수동 검증: 6개 시나리오 모두 통과
</verification>

<success_criteria>
- 매출 집계 버튼 클릭 시 오버레이가 열리고 이번 달 / 지난 달 / 직접 입력 기간별 매출 합계가 표시됨
- 미수금 > 0이면 "전체 목록 보기" 링크가 표시되고 클릭 시 openAllUnpaid() 오버레이가 열림
- 고객 선택 시 거래 요약 배지가 표시됨 (거래 없으면 숨겨짐)
- 전체 미수금 목록에 지역별 그룹 + 소계가 표시됨
- 처방이력에만 있는 고객이 자동완성에 "이력" 배지로 표시됨
- 자동완성 처방이력 건수 배지가 실제 수와 일치함
</success_criteria>

<output>
After completion, create `.planning/phases/04-sales-history/04-02-SUMMARY.md`

Note: 이 플랜은 Phase 4의 최종 브라우저 검증을 포함한다. Task 1/2가 변경 없이 검증만으로 완료될 경우에도 Task 3 브라우저 체크포인트는 필수로 실행한다.
</output>
