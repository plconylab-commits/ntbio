---
phase: 04-sales-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - customerDB.js
autonomous: true
requirements: [SALE-01, SALE-02, SALE-03]

must_haves:
  truths:
    - "saveInvoice() 실행 시 customerId가 레코드에 포함된다"
    - "saveInvoice() 실행 후 fertilizer_transactions localStorage에 미러 레코드가 저장된다"
    - "addPayment() / removePayment() 실행 후 fertilizer_transactions 미러 레코드가 동기화된다"
    - "CustomerDB.listTransactions()로 기간/고객 필터 조회가 가능하다"
    - "CustomerDB.getCustomerSummary()로 고객별 총거래/미수금 집계를 조회할 수 있다"
  artifacts:
    - path: "index.html"
      provides: "saveInvoice에 customerId 추가 + 미러 쓰기 호출"
      contains: "CustomerDB.saveTransaction"
    - path: "customerDB.js"
      provides: "saveTransaction, updateTransaction, listTransactions, getCustomerSummary 메서드"
      exports: ["saveTransaction", "updateTransaction", "listTransactions", "getCustomerSummary"]
  key_links:
    - from: "index.html saveInvoice()"
      to: "CustomerDB.saveTransaction()"
      via: "미러 쓰기 호출"
      pattern: "CustomerDB\\.saveTransaction"
    - from: "index.html addPayment()"
      to: "CustomerDB.updateTransaction()"
      via: "납부 상태 동기화"
      pattern: "CustomerDB\\.updateTransaction"
---

<objective>
거래 데이터 레이어 완성 — saveInvoice/addPayment/removePayment에 customerId 추가 + fertilizer_transactions 미러 쓰기 + CustomerDB 거래 조회 메서드 구현

Purpose: SALE-01~03의 데이터 기반을 구축하여, Plan 02의 매출 집계 UI와 고객별 요약 UI가 CustomerDB 메서드를 호출할 수 있게 한다.
Output: index.html 수정 (customerId + 미러 호출), customerDB.js 확장 (거래 CRUD 4개 메서드)
</objective>

<execution_context>
@/Users/glen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sales-history/04-RESEARCH.md

<interfaces>
<!-- Key interfaces the executor needs -->

From index.html (lines 1780-1789):
```javascript
function customerKey(name,phone){
  return (name.trim()+'|'+phone.replace(/\D/g,'')).toLowerCase();
}
function getInvoiceHistory(){
  return _invoiceCache||[];
}
function setInvoiceHistory(list){
  _invoiceCache=list;
  idbWrite(list).catch(e=>console.error('IDB 저장 실패:',e));
}
```

From index.html saveInvoice() (lines 1891-1937):
```javascript
function saveInvoice(){
  // ... builds record object ...
  const record={
    id:now+'_'+Math.floor(Math.random()*10000),
    savedAt:new Date().toISOString(),
    status:'미입금',
    paidAt:null,
    paidAmount:0,
    payments:[],
    customer:{ name, phone, phone2, addr, region, area, crop, date },
    items:cart.map(c=>({name:c.name,size:c.size,retail:c.retail,sp:c.sp,qty:c.qty,gift:c.gift||false,date:c.date||''})),
    totals:{retail,supply,vatIncluded:vm>1,vatAmount:vatAmt,grandTotal}
  };
  history.push(record);
  setInvoiceHistory(history);
  updateUnpaidBadge();
  pushToServer('save',record);
}
```

From customerUI.js (line 506):
```javascript
getCurrentCustomerId: function() { return _currentCustomerId; },
```

From customerDB.js KEYS:
```javascript
var KEYS = {
  customers:     'fertilizer_customers',
  prescriptions: 'fertilizer_prescriptions',
  transactions:  'fertilizer_transactions',
  version:       'fertilizer_schema_version'
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CustomerDB 거래 CRUD 메서드 추가</name>
  <files>customerDB.js</files>
  <read_first>customerDB.js, index.html (lines 1780-1990)</read_first>
  <action>
customerDB.js의 `window.CustomerDB` 객체에 다음 4개 메서드를 추가한다. 기존 `deletePrescription` 메서드와 `exportAll` 메서드 사이에 삽입한다.

**1. saveTransaction(record)**
- `fertilizer_transactions` localStorage 키에 레코드를 push
- record는 index.html saveInvoice()가 생성하는 그대로의 객체 (id, savedAt, status, customer, items, totals 등)
- 동일 id가 이미 존재하면 덮어쓰기 (updateTransaction 로직과 동일)
- 구현:
```javascript
saveTransaction: function(record) {
  if (!record || !record.id) return;
  var list = _get(KEYS.transactions);
  var idx = -1;
  for (var i = 0; i < list.length; i++) {
    if (list[i].id === record.id) { idx = i; break; }
  }
  if (idx !== -1) { list[idx] = record; }
  else { list.push(record); }
  _set(KEYS.transactions, list);
},
```

**2. updateTransaction(record)**
- id로 기존 레코드를 찾아 교체. 없으면 push.
- 구현은 saveTransaction과 동일한 로직 (내부에서 saveTransaction 호출 가능)
```javascript
updateTransaction: function(record) {
  this.saveTransaction(record);
},
```

**3. listTransactions(opts)**
- opts: `{ name, phone, dateFrom, dateTo, statusFilter }` (모두 optional)
- statusFilter 없으면 `'삭제됨'` 제외
- statusFilter가 'unpaid'이면 `'미입금'` 또는 `'일부입금'`만 포함
- dateFrom/dateTo는 `'YYYY-MM-DD'` 문자열 — `savedAt.slice(0,10)` 과 lexicographic 비교
- name+phone이 있으면 `customerKey(name, phone)` 비교 — customerKey가 index.html 전역 함수이므로, customerDB.js 내부에서는 동일 로직을 인라인으로 구현:
  `(h.customer.name.trim()+'|'+h.customer.phone.replace(/\D/g,'')).toLowerCase()`
- 최신순 정렬 (savedAt 내림차순)
```javascript
listTransactions: function(opts) {
  opts = opts || {};
  var list = _get(KEYS.transactions);
  list = list.filter(function(h) {
    if (!opts.statusFilter && h.status === '삭제됨') return false;
    if (opts.statusFilter === 'unpaid' && h.status !== '미입금' && h.status !== '일부입금') return false;
    if (opts.dateFrom) {
      var d = (h.savedAt || '').slice(0, 10);
      if (d < opts.dateFrom) return false;
    }
    if (opts.dateTo) {
      var d2 = (h.savedAt || '').slice(0, 10);
      if (d2 > opts.dateTo) return false;
    }
    if (opts.name && opts.phone) {
      var qKey = (opts.name.trim() + '|' + opts.phone.replace(/\D/g, '')).toLowerCase();
      var hKey = ((h.customer && h.customer.name || '').trim() + '|' + (h.customer && h.customer.phone || '').replace(/\D/g, '')).toLowerCase();
      if (hKey !== qKey) return false;
    }
    return true;
  });
  return list.sort(function(a, b) { return (b.savedAt || '').localeCompare(a.savedAt || ''); });
},
```

**4. getCustomerSummary(name, phone)**
- name+phone 기반 필터 (customerKey 인라인 구현)
- 삭제됨 제외
- 반환: `{ count, totalAmount, totalPaid, unpaid, invoices }`
```javascript
getCustomerSummary: function(name, phone) {
  var qKey = (name.trim() + '|' + phone.replace(/\D/g, '')).toLowerCase();
  var invoices = _get(KEYS.transactions).filter(function(h) {
    if (h.status === '삭제됨') return false;
    var hKey = ((h.customer && h.customer.name || '').trim() + '|' + (h.customer && h.customer.phone || '').replace(/\D/g, '')).toLowerCase();
    return hKey === qKey;
  });
  var totalAmount = invoices.reduce(function(s, h) { return s + (h.totals && h.totals.grandTotal || 0); }, 0);
  var totalPaid = invoices.reduce(function(s, h) { return s + (h.paidAmount || 0); }, 0);
  return {
    count: invoices.length,
    totalAmount: totalAmount,
    totalPaid: totalPaid,
    unpaid: totalAmount - totalPaid,
    invoices: invoices
  };
}
```

모든 메서드는 기존 IIFE 내부의 `_get`, `_set`, `KEYS` 클로저 변수를 사용한다.
  </action>
  <verify>
    <automated>grep -c "saveTransaction\|updateTransaction\|listTransactions\|getCustomerSummary" customerDB.js | grep -q "^[4-9]" && echo "PASS: 4+ method references found" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `grep "saveTransaction: function" customerDB.js` 가 매치됨
- `grep "updateTransaction: function" customerDB.js` 가 매치됨
- `grep "listTransactions: function" customerDB.js` 가 매치됨
- `grep "getCustomerSummary: function" customerDB.js` 가 매치됨
- `grep "KEYS.transactions" customerDB.js` 가 4회 이상 매치됨 (기존 exportAll/importAll 포함)
- `grep "삭제됨" customerDB.js` 가 매치됨 (status 필터)
- 기존 exportAll, importAll 함수가 변경 없이 유지됨
  </acceptance_criteria>
  <done>CustomerDB에 saveTransaction, updateTransaction, listTransactions, getCustomerSummary 4개 메서드가 추가되어 fertilizer_transactions을 통한 거래 CRUD + 집계 조회가 가능하다</done>
</task>

<task type="auto">
  <name>Task 2: saveInvoice/addPayment/removePayment에 customerId + 미러 쓰기 추가</name>
  <files>index.html</files>
  <read_first>index.html (lines 1891-1975), customerDB.js</read_first>
  <action>
index.html의 3개 함수를 최소 수정한다.

**1. saveInvoice() (line ~1914):**
record 객체에 `customerId` 필드를 추가한다. `totals` 줄 바로 위에 삽입:
```javascript
    customerId: (typeof CustomerUI !== 'undefined' && CustomerUI.getCurrentCustomerId)
      ? CustomerUI.getCurrentCustomerId() : null,
```

`pushToServer('save',record);` 줄 바로 다음에 미러 쓰기 추가:
```javascript
  if (typeof CustomerDB !== 'undefined' && CustomerDB.saveTransaction) {
    CustomerDB.saveTransaction(record);
  }
```

**2. addPayment() (line ~1957):**
`pushToServer('update',inv);` 줄 바로 다음에 미러 동기화 추가:
```javascript
  if (typeof CustomerDB !== 'undefined' && CustomerDB.updateTransaction) {
    CustomerDB.updateTransaction(inv);
  }
```

**3. removePayment() (line ~1974):**
`pushToServer('update',inv);` 줄 바로 다음에 미러 동기화 추가:
```javascript
  if (typeof CustomerDB !== 'undefined' && CustomerDB.updateTransaction) {
    CustomerDB.updateTransaction(inv);
  }
```

**주의사항:**
- 기존 코드 줄을 삭제하지 않는다 — 각각 1~3줄 추가만 한다
- `typeof` 가드를 반드시 사용한다 (CustomerDB/CustomerUI 미로드 시 안전)
- pushToServer() 호출 순서를 변경하지 않는다 (기존 서버 동기화 우선)
  </action>
  <verify>
    <automated>grep -c "CustomerDB.saveTransaction\|CustomerDB.updateTransaction" index.html | grep -q "^[3-9]" && echo "PASS: 3+ mirror calls found" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `grep "customerId:" index.html` 가 saveInvoice 내부에서 매치됨
- `grep "CustomerUI.getCurrentCustomerId" index.html` 가 saveInvoice 내부에서 매치됨
- `grep "CustomerDB.saveTransaction" index.html` 가 saveInvoice 내부에서 매치됨
- `grep "CustomerDB.updateTransaction" index.html` 가 addPayment/removePayment에서 각각 매치됨 (총 2회)
- `grep "typeof CustomerDB" index.html` 가 3회 매치됨 (saveInvoice 1회, addPayment 1회, removePayment 1회)
- 기존 pushToServer 호출이 모두 보존됨
- 기존 setInvoiceHistory / updateUnpaidBadge 호출이 모두 보존됨
  </acceptance_criteria>
  <done>saveInvoice가 customerId를 레코드에 포함하고, saveInvoice/addPayment/removePayment 모두 fertilizer_transactions에 미러 쓰기를 수행한다</done>
</task>

</tasks>

<verification>
1. `grep "saveTransaction: function" customerDB.js` — 메서드 존재 확인
2. `grep "CustomerDB.saveTransaction" index.html` — 미러 호출 확인
3. `grep "customerId:" index.html` — customerId 필드 확인
4. 브라우저 콘솔에서 `CustomerDB.listTransactions()` 호출 가능 확인
5. 브라우저 콘솔에서 `JSON.parse(localStorage.getItem('fertilizer_transactions'))` 확인
</verification>

<success_criteria>
- CustomerDB에 saveTransaction, updateTransaction, listTransactions, getCustomerSummary 메서드 존재
- saveInvoice 실행 시 customerId가 record에 포함되고 fertilizer_transactions에 미러 저장됨
- addPayment/removePayment 실행 시 fertilizer_transactions의 해당 레코드가 동기화됨
- listTransactions({statusFilter:'unpaid'})로 미납 거래만 필터 가능
- getCustomerSummary(name, phone)로 고객별 집계 가능
</success_criteria>

<output>
After completion, create `.planning/phases/04-sales-history/04-01-SUMMARY.md`
</output>
