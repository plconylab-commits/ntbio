---
phase: 10-prescription-pdf-parsing-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 처방전.html
  - index.html
autonomous: true
requirements:
  - PARSE-10
  - PARSE-11
  - PARSE-12
  - PARSE-13
  - PARSE-14
  - SYNC-01
  - SYNC-02

must_haves:
  truths:
    - "깻잎(400평) 처방전 PDF의 평당가(1,640원)가 `_isCostPage`를 통과하여 추출된다"
    - "index.html에서 처방전 PDF 업로드 후 '처방전 버튼을 눌러 처방전을 확인하세요' 토스트가 표시된다"
    - "SYNC-02: goBackToCart() 함수가 존재하며 rxData를 localStorage에 저장 후 index.html로 이동한다"
  artifacts:
    - path: "처방전.html"
      provides: "_COST_KW에 '평당' 포함 (총 8개 키워드)"
      contains: "'소매가','공급가','단가','합계','총액','금액','계','평당'"
    - path: "index.html"
      provides: "처방전 PDF 업로드 후 네비게이션 안내 토스트"
      contains: "처방전 버튼"
  key_links:
    - from: "index.html onPdfSelected (3순위 경로)"
      to: "showToast()"
      via: "rxPendingPdf 저장 직후 showToast 호출"
      pattern: "rxPendingPdf.*showToast|showToast.*처방전"
    - from: "_isCostPage(texts)"
      to: "_COST_KW"
      via: "filter count >= _COST_KW_MIN(3)"
      pattern: "_COST_KW.*평당|평당.*_COST_KW"
---

<objective>
깻잎 처방전 PDF의 평당가 감지 실패를 수정하고, index.html에서 처방전 PDF 업로드 후 사용자에게 처방전 화면으로 이동하도록 안내하는 토스트를 추가한다.

Purpose: PARSE-14(깻잎 style 평당가 추출)와 SYNC-01(업로드 후 UX 안내)의 마지막 코드 공백을 닫는다. PARSE-10~13, SYNC-02는 이미 커밋된 코드로 구현 완료 — 본 플랜에서 코드 확인만 수행한다.
Output: 처방전.html의 `_COST_KW` 수정본, index.html의 처방전 안내 토스트
</objective>

<execution_context>
@/Users/glen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-prescription-pdf-parsing-sync/10-RESEARCH.md

<interfaces>
<!-- 처방전.html — Cost page detection constants (line 4827-4828) -->
```javascript
const _COST_KW     = ['소매가','공급가','단가','합계','총액','금액','계'];
const _COST_KW_MIN = 3;

function _isCostPage(texts) {
  const all = texts.join(' ');
  return _COST_KW.filter(kw => all.includes(kw)).length >= _COST_KW_MIN;
}
```

<!-- index.html — rxPendingPdf save path (lines 2764-2773) -->
```javascript
// 3순위: 나머지 모든 PDF → Vision AI (스캔 처방전 포함)
// 처방전.html 단계 복원용: PDF 바이너리를 base64로 저장 → 좌표 기반 파싱
try {
  let binary = '';
  for (let i = 0; i < data.length; i++) binary += String.fromCharCode(data[i]);
  localStorage.setItem('rxPendingPdf', btoa(binary));
} catch(e) { console.warn('[rxPendingPdf] 저장 실패:', e); }
localStorage.removeItem('rxStages');
handlePrescriptionUpload(file);
```

<!-- index.html — showToast signature (example usage) -->
```javascript
showToast('처방전 불러오기 완료 — ${cart.length}개 제품, ${stageCount}단계 연동됨 ✓');
// second arg = true → error (red), omit = info (normal)
```

<!-- 깻잎 cost line format — "합계=656,000,원 평당(1,640원)" -->
<!-- _COST_KW keywords matched: '합계'(1) + '계'(1, substring of '합계') = 2 total  -->
<!-- Adding '평당' → '합계'(1) + '계'(1) + '평당'(1) = 3 → passes _COST_KW_MIN = 3 ✓ -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: _COST_KW에 '평당' 추가 — 깻잎 style 평당가 감지 수정</name>
  <files>처방전.html</files>
  <read_first>
    - 처방전.html line 4827: `const _COST_KW = [...]` — 현재 배열 확인
    - 처방전.html line 4828: `const _COST_KW_MIN = 3;` — 임계값 확인 (변경하지 않음)
    - 처방전.html line 4850-4853: `_isCostPage` 함수 전체 확인
  </read_first>
  <action>
처방전.html line 4827의 `_COST_KW` 배열에 `'평당'`을 추가한다.

변경 전:
```javascript
const _COST_KW = ['소매가','공급가','단가','합계','총액','금액','계'];
```

변경 후:
```javascript
const _COST_KW = ['소매가','공급가','단가','합계','총액','금액','계','평당'];
```

이유: 깻잎(400평) 처방전의 평당가 행 "합계=656,000,원 평당(1,640원)"은 '합계'(1) + '계'(1, substring) = 2 키워드만 매칭되어 임계값 3에 미달. '평당' 추가 시 = 3으로 통과. `_COST_KW_MIN`은 3으로 유지한다.

주의: '계'가 '합계'의 substring으로 이미 카운트되는지 확인 필요 — `all.includes('계')`는 '합계' 문자열에서도 true를 반환하므로 2개로 맞음. '평당' 추가로 3개 달성.
  </action>
  <verify>
    <automated>grep -n "_COST_KW" /Users/glen/천연비료처방전/천연비료처방전/.claude/worktrees/hopeful-kepler/처방전.html | grep "평당"</automated>
  </verify>
  <acceptance_criteria>
    - `처방전.html`의 `_COST_KW` 배열에 `'평당'`이 포함됨
    - `_COST_KW_MIN`은 여전히 `3`임
    - `grep -c "'평당'" 처방전.html | grep -v "^0$"` — '평당'이 _COST_KW 배열 내에 존재
  </acceptance_criteria>
  <done>처방전.html의 `_COST_KW` 배열이 8개 요소를 가지며 '평당'이 포함됨. `_COST_KW_MIN = 3` 유지.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SYNC-01 UX — index.html 처방전 PDF 업로드 후 안내 토스트 추가</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 2764-2773: 3순위 처방전 PDF 처리 경로 전체
    - index.html lines 2670-2673: `applyRxDataToCart` 끝부분 — 기존 처방전 연동 성공 토스트 문구 패턴 참조
    - index.html: `showToast` 함수 시그니처 확인 (두 번째 파라미터 true = 에러)
  </read_first>
  <action>
index.html의 3순위 처방전 PDF 경로(line 2773 `handlePrescriptionUpload(file)` 호출 직전)에 안내 토스트를 추가한다.

현재 코드 (lines 2767-2773):
```javascript
try {
  let binary = '';
  for (let i = 0; i < data.length; i++) binary += String.fromCharCode(data[i]);
  localStorage.setItem('rxPendingPdf', btoa(binary));
} catch(e) { console.warn('[rxPendingPdf] 저장 실패:', e); }
localStorage.removeItem('rxStages');
handlePrescriptionUpload(file);
```

변경 후 — `localStorage.removeItem('rxStages')` 다음 줄에 토스트 삽입:
```javascript
try {
  let binary = '';
  for (let i = 0; i < data.length; i++) binary += String.fromCharCode(data[i]);
  localStorage.setItem('rxPendingPdf', btoa(binary));
} catch(e) { console.warn('[rxPendingPdf] 저장 실패:', e); }
localStorage.removeItem('rxStages');
showToast('처방전 PDF 저장됨 — 상단 [처방전 처방] 버튼을 눌러 단계를 확인하세요');
handlePrescriptionUpload(file);
```

이유: 현재는 `handlePrescriptionUpload`가 Vision AI 결과를 거래명세표에 적용하는 토스트(line 2596)를 표시하지만, `rxPendingPdf`가 처방전.html에서 자동으로 파싱된다는 안내가 없다. 사용자가 [처방전 처방] 버튼을 눌러야 처방전.html로 이동해 단계가 복원된다는 것을 알아야 한다.

주의: `handlePrescriptionUpload`가 별도의 toast를 출력하므로 이 토스트는 추가 안내용이다. 중복처럼 보일 수 있지만 서로 다른 정보를 전달한다 — Vision AI 결과(기존) vs 처방전 단계 복원 안내(신규).
  </action>
  <verify>
    <automated>grep -n "처방전 처방.*버튼\|처방전 PDF 저장됨" /Users/glen/천연비료처방전/천연비료처방전/.claude/worktrees/hopeful-kepler/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `index.html`에 `showToast('처방전 PDF 저장됨` 문자열이 존재함
    - 해당 showToast 호출이 `localStorage.removeItem('rxStages')` 직후이고 `handlePrescriptionUpload(file)` 직전에 위치함
    - grep으로 위치 순서 확인: `grep -n "removeItem.*rxStages\|처방전 PDF 저장됨\|handlePrescriptionUpload" index.html` — 줄 번호가 오름차순임
  </acceptance_criteria>
  <done>index.html의 3순위 처방전 PDF 경로에서 `rxPendingPdf` 저장 직후 처방전 화면 이동 안내 토스트가 표시됨.</done>
</task>

</tasks>

<verification>
두 수정 모두 단일 상수/단일 줄 변경이다. 회귀 위험 없음.

1. `grep -n "_COST_KW" 처방전.html` — '평당'이 포함된 8-요소 배열 확인
2. `grep -n "처방전 PDF 저장됨" index.html` — 토스트 문자열 존재 확인
3. `grep -n "removeItem.*rxStages\|처방전 PDF 저장됨\|handlePrescriptionUpload" index.html` — 줄 번호 오름차순 (removeItem < toast < handlePrescriptionUpload)
</verification>

<success_criteria>
- 처방전.html `_COST_KW`에 '평당' 포함 (8개 요소)
- index.html 3순위 경로에 처방전 화면 이동 안내 토스트 존재
- 기존 기능(PARSE-10~13, SYNC-02)은 이미 커밋된 코드로 완료 — 코드 변경 없음
</success_criteria>

<output>
After completion, create `.planning/phases/10-prescription-pdf-parsing-sync/10-01-SUMMARY.md`
</output>
