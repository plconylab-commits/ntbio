---
phase: 01-pdf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pdfParser.js
  - main.js
  - uiController.js
autonomous: true
requirements:
  - PARSE-01
  - PARSE-02
  - PARSE-03
  - PARSE-04

must_haves:
  truths:
    - "복합형 처방전(천혜향 등)을 파싱하면 최종 합계 행의 평당가만 추출된다"
    - "비용 페이지 이후 광고/홍보 페이지가 처방 항목에 나타나지 않는다"
    - "파싱 오류 시 사용자에게 재시도 안내 메시지가 표시된다"
    - "홍보용 항목은 장바구니에 금액 0원으로 추가된다"
  artifacts:
    - path: "pdfParser.js"
      provides: "extractCostPageData() 합계 행 우선 평당가 추출 + 비용 페이지 이후 break"
      contains: "pyeongFromTotal"
    - path: "main.js"
      provides: "파싱 실패 시 재시도 안내 메시지"
      contains: "다시 시도"
    - path: "uiController.js"
      provides: "홍보용 항목 0원 처리"
      contains: "홍보용"
  key_links:
    - from: "pdfParser.js"
      to: "main.js"
      via: "parsePdfToJSON() 반환값 → handlePrescriptionUpload() costData 소비"
      pattern: "costData"
    - from: "uiController.js"
      to: "cart"
      via: "_applyToCart() → cart.push 시 홍보용 가격 0 강제"
      pattern: "홍보용.*0"
---

<objective>
PDF 파싱 버그 4건을 수정하여 처방전 데이터 정확성을 확보한다.

Purpose: 이후 Phase 2-4의 고객 DB, 처방이력, 거래이력이 모두 정확한 파싱 데이터에 의존하므로 데이터 정확성이 최우선이다.
Output: pdfParser.js, main.js, uiController.js 수정 완료
</objective>

<execution_context>
@/Users/glen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pdf/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PARSE-01 평당가 중복 덮어쓰기 + PARSE-02 광고 페이지 스킵</name>
  <files>pdfParser.js</files>
  <read_first>
    - pdfParser.js (전체 — 특히 lines 75-144 extractCostPageData 함수, lines 500-515 비용 페이지 감지 로직)
    - .planning/phases/01-pdf/01-RESEARCH.md (PARSE-01, PARSE-02 섹션)
  </read_first>
  <action>
**PARSE-01 수정 (extractCostPageData 함수, lines 75-144):**

1. 변수 선언부(line 81-83)에 2개 변수 추가:
   - 기존: `let totalCost = null; let unitPricePerPyeong = null; let supplySum = 0;`
   - 변경: 기존 3개 유지 + `let pyeongFromTotal = null;` + `let pyeongLastSeen = null;` 추가

2. 평당 단가 추출 블록(lines 94-98) 교체:
   - 기존:
     ```javascript
     if (PYEONG_RE.test(text)) {
       const nums = parseNums(text, 100, 10_000_000);
       if (nums.length) unitPricePerPyeong = nums[nums.length - 1];
     }
     ```
   - 변경:
     ```javascript
     if (PYEONG_RE.test(text)) {
       const nums = parseNums(text, 100, 10_000_000);
       if (nums.length) {
         const val = nums[nums.length - 1];
         pyeongLastSeen = val;
         if (TOTAL_RE.test(text)) {
           pyeongFromTotal = val;
         }
       }
     }
     ```

3. return 문(line 143) 직전에 최종 결정 1줄 추가:
   ```javascript
   unitPricePerPyeong = pyeongFromTotal !== null ? pyeongFromTotal : pyeongLastSeen;
   return { totalCost, unitPricePerPyeong };
   ```

**PARSE-02 수정 (비용 페이지 감지 후 break, line 513):**

line 513의 `continue;`를 `break;`로 변경하고 로그 메시지도 갱신:
- 기존: `continue; // 이 페이지는 처방 단계로 처리하지 않음`
- 변경: `break; // 비용 페이지 이후 광고/홍보 페이지 포함 모든 페이지 스킵 (PARSE-02)`

line 510의 console.log 메시지도 변경:
- 기존: `단계 파싱 스킵`
- 변경: `이후 모든 페이지 스킵 (break)`

**PARSE-03 부분 수정 (pdfParser.js line 1053 + line 1112):**

line 1053의 alert 메시지 변경:
- 기존: `'처방전에서 텍스트를 추출할 수 없습니다.\n스캔 이미지 PDF는 지원되지 않습니다.'`
- 변경: `'처방전에서 텍스트를 추출할 수 없습니다.\n\n다시 시도해 주세요.\n스캔 이미지(사진) PDF인 경우 텍스트 PDF로 변환 후 업로드해 주세요.'`

line 1112의 alert 메시지 변경:
- 기존: `` alert(`처방전 파싱 오류\n\n${e.name}: ${e.message}`); ``
- 변경: `` alert(`처방전 파싱 중 오류가 발생했습니다.\n\n다시 시도해 주세요.\n반복 실패 시 PDF를 새로 저장하거나 담당자에게 문의하세요.`); ``
  </action>
  <verify>
    <automated>grep -c "pyeongFromTotal" pdfParser.js && grep -c "pyeongLastSeen" pdfParser.js && grep "break;" pdfParser.js | grep -c "비용\|광고\|스킵" && grep -c "다시 시도" pdfParser.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "pyeongFromTotal" pdfParser.js` returns at least 3 matches (declaration, assignment, final decision)
    - `grep "pyeongLastSeen" pdfParser.js` returns at least 2 matches (declaration, assignment)
    - `grep "break;" pdfParser.js` shows a break after `costData = extractCostPageData(rows)` line
    - `grep "다시 시도" pdfParser.js` returns at least 2 matches (line 1053 and 1112 areas)
    - `grep "unitPricePerPyeong = pyeongFromTotal" pdfParser.js` returns exactly 1 match
  </acceptance_criteria>
  <done>extractCostPageData()가 합계 행 평당가를 우선 추출하고, 비용 페이지 이후 모든 페이지가 skip되며, 파싱 오류 시 재시도 안내가 표시된다</done>
</task>

<task type="auto">
  <name>Task 2: PARSE-03 main.js 에러 메시지 개선 + PARSE-04 홍보용 항목 0원 처리</name>
  <files>main.js, uiController.js</files>
  <read_first>
    - main.js (전체 — 특히 lines 82-85 catch 블록)
    - uiController.js (lines 1395-1465 — mergedMap 구성 및 cart.push 로직)
    - .planning/phases/01-pdf/01-RESEARCH.md (PARSE-03, PARSE-04 섹션)
  </read_first>
  <action>
**PARSE-03: main.js catch 블록 개선 (lines 82-85):**

기존 catch 블록:
```javascript
} catch (err) {
  console.error('처방전 처리 오류:', err);
  alert('처방전 처리 중 오류가 발생했습니다: ' + err.message);
}
```

변경 후:
```javascript
} catch (err) {
  console.error('처방전 처리 오류:', err);
  const msg = err.message || '';
  if (msg.includes('JSON') || msg.includes('parse') || msg.includes('Unexpected token')) {
    alert('처방전 파싱에 실패했습니다.\n\n다시 시도해 주세요.\n반복 실패 시 PDF를 새로 저장하거나 담당자에게 문의하세요.');
  } else {
    alert('처방전 처리 중 오류가 발생했습니다.\n\n다시 시도해 주세요.\n' + msg);
  }
}
```

핵심: `다시 시도` 문구가 모든 에러 경로에 포함되어야 한다.

**PARSE-04: uiController.js 홍보용 항목 0원 처리:**

`_applyToCart()` 함수의 STEP 5 장바구니 적용 루프(lines 1434-1464) 내에서, `cart.push` 시 가격 결정 부분을 수정한다.

line 1444 (`const existing = cart.find(...)`) 직전에 홍보용 감지 + 가격 오버라이드 로직 삽입:

```javascript
    // PARSE-04: 홍보용 항목 금액 0원 처리
    // mergedMap의 matchId에 해당하는 원본 행에서 홍보용 여부 확인
    const sourceRows = rows.filter(r => r.matchId === matchId);
    const isPromo = sourceRows.some(r =>
      /홍보용/.test(r.productRaw || '') ||
      /홍보용/.test(r.productName || '') ||
      /홍보용/.test(r.stageRaw || '')
    );
    const priceOverride = isPromo ? 0 : (prod.price || 0);
```

그리고 기존 cart.push 객체에서 가격 필드를 `priceOverride`로 교체:
- line 1454: `retail: prod.price || 0,` → `retail: priceOverride,`
- line 1456: `sp: prod.price || 0,` → `sp: priceOverride,`

기존 항목 수량 추가 경로(line 1446-1448, `existing` 분기)에서도 홍보용이면 가격을 0으로:
```javascript
    if (existing) {
      existing.qty += totalQty;
      if (isPromo) { existing.retail = 0; existing.sp = 0; }
      console.log(`[_applyToCart] 기존 항목 수량 추가: "${prod.name}" +${totalQty} → ${existing.qty}${isPromo ? ' (홍보용 0원)' : ''}`);
    }
```

새 항목 push 로그에도 홍보용 표시 추가:
- line 1461: `console.log` 끝에 `${isPromo ? ' (홍보용 0원)' : ''}` 추가
  </action>
  <verify>
    <automated>grep -c "다시 시도" main.js && grep -c "홍보용" uiController.js && grep -c "priceOverride" uiController.js && grep -c "isPromo" uiController.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "다시 시도" main.js` returns at least 2 matches (JSON parse 경로 + 일반 오류 경로)
    - `grep "홍보용" uiController.js` returns at least 2 matches (정규식 테스트 부분)
    - `grep "isPromo" uiController.js` returns at least 3 matches (선언, 가격 결정, 기존 항목 분기)
    - `grep "priceOverride" uiController.js` returns at least 3 matches (선언, retail 할당, sp 할당)
    - `grep "Unexpected token" main.js` returns at least 1 match (JSON 파싱 실패 감지)
  </acceptance_criteria>
  <done>main.js의 모든 에러 경로에 "다시 시도" 안내가 포함되고, uiController.js에서 홍보용 항목이 금액 0원으로 장바구니에 추가된다</done>
</task>

</tasks>

<verification>
1. `grep -c "pyeongFromTotal" pdfParser.js` >= 3
2. `grep "break;" pdfParser.js | grep -c "스킵\|비용\|광고"` >= 1
3. `grep -c "다시 시도" pdfParser.js` >= 2
4. `grep -c "다시 시도" main.js` >= 2
5. `grep -c "홍보용" uiController.js` >= 2
6. `grep -c "priceOverride" uiController.js` >= 3
7. `node -e "require('./pdfParser.js')" 2>&1` exits without error (basic syntax check)
</verification>

<success_criteria>
- PARSE-01: extractCostPageData()가 pyeongFromTotal(합계 행)을 우선하고 pyeongLastSeen을 fallback으로 사용한다
- PARSE-02: 비용 페이지 감지 후 break로 이후 모든 페이지(광고/홍보 포함) 처리를 중단한다
- PARSE-03: parsePdfToJSON 오류, main.js catch, 텍스트 미추출 모든 경로에서 "다시 시도" 안내가 표시된다
- PARSE-04: 홍보용 항목이 장바구니에 retail=0, sp=0으로 추가된다
</success_criteria>

<output>
After completion, create `.planning/phases/01-pdf/01-01-SUMMARY.md`
</output>
