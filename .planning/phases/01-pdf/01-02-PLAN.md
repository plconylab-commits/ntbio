---
phase: 01-pdf
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - customerDB.js
  - index.html
autonomous: true
requirements:
  - PARSE-01
  - PARSE-02
  - PARSE-03
  - PARSE-04

must_haves:
  truths:
    - "localStorage에 fertilizer_customers, fertilizer_prescriptions, fertilizer_transactions 키가 빈 배열로 초기화된다"
    - "JSON 내보내기 버튼 클릭 시 전체 데이터가 .json 파일로 다운로드된다"
    - "JSON 가져오기 버튼으로 백업 파일을 업로드하면 데이터가 복원된다"
  artifacts:
    - path: "customerDB.js"
      provides: "localStorage 스키마 초기화 + CRUD 기반 + exportAll/importAll"
      exports: ["window.CustomerDB"]
      contains: "fertilizer_customers"
    - path: "index.html"
      provides: "내보내기/가져오기 버튼 + customerDB.js script 태그"
      contains: "customerDB.js"
  key_links:
    - from: "customerDB.js"
      to: "localStorage"
      via: "_migrate() 자동 호출로 3키 초기화"
      pattern: "fertilizer_customers"
    - from: "index.html"
      to: "customerDB.js"
      via: "script src 태그로 로드 + 버튼 onclick으로 exportAll/importAll 호출"
      pattern: "CustomerDB"
---

<objective>
localStorage 기반 데이터 인프라(스키마 초기화 + JSON 내보내기/가져오기)를 구축한다.

Purpose: Phase 2-4에서 고객 DB, 처방이력, 거래이력을 저장할 기반 구조. 데이터 백업/복원 수단도 함께 제공.
Output: customerDB.js 신규 파일 + index.html 버튼 2개 및 script 태그 추가
</objective>

<execution_context>
@/Users/glen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pdf/01-RESEARCH.md

<interfaces>
<!-- index.html 기존 script 로드 순서 (lines 788-799) -->
<!-- customerDB.js는 main.js(line 796) 이후에 추가해야 함 -->
From index.html:
```html
<script src="prescriptionModel.js?v=2"></script>
<script src="productDB.js?v=2"></script>
<script src="productMapper.js?v=3"></script>
<script src="rxNormalizer.js?v=14"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.min.js"></script>
<script src="pdfParser.js?v=16"></script>
<script src="uiController.js?v=3"></script>
<script src="main.js?v=2"></script>
```

<!-- index.html .actions 영역 (lines 651-671) -->
<!-- 내보내기/가져오기 버튼은 line 667 act-divider 이후 act-group 내에 추가 -->
From index.html:
```html
<div class="actions">
  <div class="act-group">
    <button class="btn-print" onclick="doPrint()">인쇄</button>
    <div class="act-row">
      <button class="btn-pdf" onclick="doPdf()">PDF 저장</button>
      <button class="btn-email" onclick="doEmail()">서버저장</button>
    </div>
  </div>
  <div class="act-divider"></div>
  <div class="act-group">
    ...PDF 업로드, 미수금 버튼들...
  </div>
  <div class="act-divider"></div>
  <div class="act-group">
    <button class="btn-rx" onclick="goToPrescription()">처방전 처방</button>
    <button class="btn-clear" onclick="clearAll()">전체 초기화</button>
  </div>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: customerDB.js 신규 작성 — localStorage 스키마 초기화 + exportAll/importAll</name>
  <files>customerDB.js</files>
  <read_first>
    - .planning/phases/01-pdf/01-RESEARCH.md (localStorage 스키마 초기화 섹션, lines 291-371)
  </read_first>
  <action>
신규 파일 `customerDB.js` 생성. IIFE 패턴으로 `window.CustomerDB` 네임스페이스 노출.

전체 코드 구조:

```javascript
/**
 * customerDB.js — localStorage 기반 고객/처방/거래 데이터 관리
 * Phase 1: 스키마 초기화 + JSON 내보내기/가져오기
 * Phase 2+: 고객 CRUD, 처방이력 저장 등 확장
 */
(function() {
  'use strict';

  const SCHEMA_VERSION = 1;
  const KEYS = {
    customers:     'fertilizer_customers',
    prescriptions: 'fertilizer_prescriptions',
    transactions:  'fertilizer_transactions',
    version:       'fertilizer_schema_version'
  };

  /** 스키마 마이그레이션 — 앱 시작 시 자동 실행 */
  function _migrate() {
    const stored = parseInt(localStorage.getItem(KEYS.version) || '0', 10);
    if (stored >= SCHEMA_VERSION) return;
    if (stored < 1) {
      if (!localStorage.getItem(KEYS.customers))
        localStorage.setItem(KEYS.customers,     JSON.stringify([]));
      if (!localStorage.getItem(KEYS.prescriptions))
        localStorage.setItem(KEYS.prescriptions, JSON.stringify([]));
      if (!localStorage.getItem(KEYS.transactions))
        localStorage.setItem(KEYS.transactions,  JSON.stringify([]));
      localStorage.setItem(KEYS.version, String(SCHEMA_VERSION));
    }
    console.log('[CustomerDB] 스키마 초기화 완료 (v' + SCHEMA_VERSION + ')');
  }

  /** localStorage JSON 안전 읽기 */
  function _get(key) {
    try { return JSON.parse(localStorage.getItem(key) || '[]'); }
    catch(e) { console.error('[CustomerDB] read error:', key, e); return []; }
  }

  /** localStorage JSON 쓰기 */
  function _set(key, value) {
    localStorage.setItem(key, JSON.stringify(value));
  }

  window.CustomerDB = {
    // Phase 2에서 구현할 CRUD 스텁
    list:       function() { return _get(KEYS.customers); },
    findById:   function(id) { return _get(KEYS.customers).find(function(c) { return c.id === id; }) || null; },
    findByName: function(name) { return _get(KEYS.customers).find(function(c) { return c.name && c.name.includes(name); }) || null; },
    save:       function(c) { /* Phase 2 */ },
    delete:     function(id) { /* Phase 2 */ },

    /** 전체 데이터 내보내기 — JSON 객체 반환 */
    exportAll: function() {
      return {
        version:       SCHEMA_VERSION,
        exportedAt:    new Date().toISOString(),
        customers:     _get(KEYS.customers),
        prescriptions: _get(KEYS.prescriptions),
        transactions:  _get(KEYS.transactions)
      };
    },

    /** 전체 데이터 가져오기 — JSON 객체 입력 */
    importAll: function(data) {
      if (!data || typeof data.version === 'undefined') {
        throw new Error('유효하지 않은 백업 파일입니다.');
      }
      _set(KEYS.customers,     Array.isArray(data.customers)     ? data.customers     : []);
      _set(KEYS.prescriptions, Array.isArray(data.prescriptions) ? data.prescriptions : []);
      _set(KEYS.transactions,  Array.isArray(data.transactions)  ? data.transactions  : []);
      console.log('[CustomerDB] 데이터 가져오기 완료');
    },

    /** 내부 테스트용 */
    _migrate: _migrate,
    _KEYS: KEYS
  };

  // 앱 시작 시 스키마 초기화
  _migrate();
})();
```

위 코드를 그대로 `customerDB.js`에 작성한다.
  </action>
  <verify>
    <automated>grep -c "fertilizer_customers" customerDB.js && grep -c "exportAll" customerDB.js && grep -c "importAll" customerDB.js && grep -c "SCHEMA_VERSION" customerDB.js && grep -c "_migrate" customerDB.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "fertilizer_customers" customerDB.js` returns at least 2 matches (KEYS 선언 + _migrate 내)
    - `grep "fertilizer_prescriptions" customerDB.js` returns at least 2 matches
    - `grep "fertilizer_transactions" customerDB.js` returns at least 2 matches
    - `grep "fertilizer_schema_version" customerDB.js` returns at least 1 match
    - `grep "exportAll" customerDB.js` returns at least 1 match (함수 정의)
    - `grep "importAll" customerDB.js` returns at least 1 match (함수 정의)
    - `grep "window.CustomerDB" customerDB.js` returns exactly 1 match
    - `grep "_migrate()" customerDB.js` returns at least 2 matches (정의 + 마지막 호출)
  </acceptance_criteria>
  <done>customerDB.js가 존재하며 3개 localStorage 키를 빈 배열로 초기화하고, exportAll/importAll 함수가 window.CustomerDB에 노출된다</done>
</task>

<task type="auto">
  <name>Task 2: index.html에 customerDB.js 로드 + 내보내기/가져오기 버튼 추가</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 647-672 actions 영역, lines 788-799 script 태그 영역)
    - .planning/phases/01-pdf/01-RESEARCH.md (JSON 내보내기/가져오기 버튼 배치 섹션)
  </read_first>
  <action>
**1. script 태그 추가 (line 796 `main.js` 다음):**

`<script src="main.js?v=2"></script>` 바로 다음 줄에 추가:
```html
<script src="customerDB.js?v=1"></script>
```

**2. 내보내기/가져오기 버튼 추가 (actions 영역):**

line 670 `<button class="btn-clear" ...>` 다음, 해당 `</div>` 닫기 전에 아래 2개 버튼 추가:

```html
      <div class="act-divider"></div>
      <div class="act-group">
        <div class="act-row">
          <button class="btn-export-json" onclick="doExportJSON()" style="background:#1565C0;color:#fff;border:none;border-radius:7px;padding:10px;font-family:inherit;font-size:13px;font-weight:700;cursor:pointer;flex:1;">JSON 내보내기</button>
          <button class="btn-import-json" onclick="document.getElementById('jsonFileInput').click()" style="background:#E3F2FD;color:#1565C0;border:1.5px solid #1565C0;border-radius:7px;padding:10px;font-family:inherit;font-size:13px;font-weight:700;cursor:pointer;flex:1;">JSON 가져오기</button>
        </div>
        <input type="file" id="jsonFileInput" accept=".json" style="display:none" onchange="doImportJSON(this)">
      </div>
```

**3. 내보내기/가져오기 JS 함수 추가:**

`</body>` 닫기 태그 직전(또는 마지막 script 태그 이후)에 인라인 script 블록 추가:

```html
<script>
/** JSON 전체 내보내기 — Blob 다운로드 */
function doExportJSON() {
  try {
    var data = CustomerDB.exportAll();
    var json = JSON.stringify(data, null, 2);
    var blob = new Blob([json], { type: 'application/json' });
    var url  = URL.createObjectURL(blob);
    var a    = document.createElement('a');
    a.href     = url;
    a.download = 'fertilizer_backup_' + new Date().toISOString().slice(0,10) + '.json';
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
    console.log('[Export] JSON 내보내기 완료');
  } catch (e) {
    alert('내보내기 실패: ' + e.message);
  }
}

/** JSON 전체 가져오기 — FileReader */
function doImportJSON(input) {
  var file = input.files && input.files[0];
  if (!file) return;
  var reader = new FileReader();
  reader.onload = function(ev) {
    try {
      var data = JSON.parse(ev.target.result);
      if (!confirm('기존 데이터를 덮어씁니다. 계속하시겠습니까?')) return;
      CustomerDB.importAll(data);
      alert('데이터 가져오기 완료!');
    } catch (e) {
      alert('가져오기 실패: ' + e.message);
    }
  };
  reader.readAsText(file);
  input.value = ''; // 같은 파일 재선택 허용
}
</script>
```

**4. 인쇄 시 내보내기/가져오기 버튼 숨김:**

index.html의 `@media print` 블록(line 324 부근)에서 `.actions` 자체가 `display:none!important`로 이미 숨겨지고 있으므로 추가 작업 불필요. 확인만 한다.
  </action>
  <verify>
    <automated>grep -c "customerDB.js" index.html && grep -c "doExportJSON" index.html && grep -c "doImportJSON" index.html && grep -c "jsonFileInput" index.html && grep -c "btn-export-json" index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep "customerDB.js" index.html` returns at least 1 match (script src 태그)
    - `grep "doExportJSON" index.html` returns at least 2 matches (function 정의 + onclick)
    - `grep "doImportJSON" index.html` returns at least 2 matches (function 정의 + onchange)
    - `grep "jsonFileInput" index.html` returns at least 2 matches (input id + getElementById)
    - `grep "btn-export-json" index.html` returns at least 1 match (내보내기 버튼)
    - `grep "btn-import-json" index.html` returns at least 1 match (가져오기 버튼)
    - `grep "CustomerDB.exportAll" index.html` returns at least 1 match
    - `grep "CustomerDB.importAll" index.html` returns at least 1 match
  </acceptance_criteria>
  <done>index.html에 customerDB.js가 로드되고, JSON 내보내기/가져오기 버튼이 actions 영역에 표시되며, doExportJSON/doImportJSON 함수가 정의되어 있다</done>
</task>

</tasks>

<verification>
1. `test -f customerDB.js && echo "OK"` outputs "OK"
2. `grep -c "fertilizer_customers" customerDB.js` >= 2
3. `grep -c "customerDB.js" index.html` >= 1
4. `grep -c "doExportJSON" index.html` >= 2
5. `grep -c "doImportJSON" index.html` >= 2
6. `grep -c "btn-export-json" index.html` >= 1
</verification>

<success_criteria>
- customerDB.js가 생성되어 fertilizer_customers/prescriptions/transactions 3개 키를 빈 배열로 초기화한다
- window.CustomerDB.exportAll()이 3키 데이터를 JSON 객체로 반환한다
- window.CustomerDB.importAll(data)이 3키 데이터를 복원한다
- index.html에 JSON 내보내기/가져오기 버튼 2개가 actions 영역에 존재한다
- 브라우저에서 내보내기 클릭 시 .json 파일이 다운로드된다
</success_criteria>

<output>
After completion, create `.planning/phases/01-pdf/01-02-SUMMARY.md`
</output>
